---
title: 下载mp3
---

# 下载 mp3

## 前端下载

无需安装 `ffmpeg`

```
channels:
  download:
    frontend: true
```


## 后台下载

需要安装 `ffmpeg`

在「视频详情页」的「更多」菜单中，有「下载为 mp3」菜单，点击即可下载 `mp3` 文件。后台下载模式下，在下载面板中显示的文件大小，和实际的会不同，因为下载为 `mp3` 文件时，会先下载 `mp4` 文件，然后使用 `ffmpeg` 转换为 `mp3` 文件。
