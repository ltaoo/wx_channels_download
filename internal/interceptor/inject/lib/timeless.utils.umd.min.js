(function(global, factory) {
	typeof exports === "object" && typeof module !== "undefined" ? factory(exports, require("@timeless/base")) : typeof define === "function" && define.amd ? define(["exports", "@timeless/base"], factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, factory((global.Timeless = global.Timeless || {}, global.Timeless.utils = {}), global.Timeless.base));
})(this, function(exports, _timeless_base) {
	Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
	var __create = Object.create;
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __getProtoOf = Object.getPrototypeOf;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __commonJSMin = (cb, mod) => () => (mod || cb((mod = { exports: {} }).exports, mod), mod.exports);
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
				get: ((k) => from[k]).bind(null, key),
				enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
		value: mod,
		enumerable: true
	}) : target, mod));
	var require_dayjs_min = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		(function(t, e) {
			"object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (t = "undefined" != typeof globalThis ? globalThis : t || self).dayjs = e();
		})(exports, (function() {
			"use strict";
			var t = 1e3, e = 6e4, n = 36e5, r = "millisecond", i = "second", s = "minute", u = "hour", a = "day", o = "week", c = "month", f = "quarter", h = "year", d = "date", l = "Invalid Date", $ = /^(\d{4})[-/]?(\d{1,2})?[-/]?(\d{0,2})[Tt\s]*(\d{1,2})?:?(\d{1,2})?:?(\d{1,2})?[.:]?(\d+)?$/, y = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, M = {
				name: "en",
				weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"),
				months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_"),
				ordinal: function(t) {
					var e = [
						"th",
						"st",
						"nd",
						"rd"
					], n = t % 100;
					return "[" + t + (e[(n - 20) % 10] || e[n] || e[0]) + "]";
				}
			}, m = function(t, e, n) {
				var r = String(t);
				return !r || r.length >= e ? t : "" + Array(e + 1 - r.length).join(n) + t;
			}, v = {
				s: m,
				z: function(t) {
					var e = -t.utcOffset(), n = Math.abs(e), r = Math.floor(n / 60), i = n % 60;
					return (e <= 0 ? "+" : "-") + m(r, 2, "0") + ":" + m(i, 2, "0");
				},
				m: function t(e, n) {
					if (e.date() < n.date()) return -t(n, e);
					var r = 12 * (n.year() - e.year()) + (n.month() - e.month()), i = e.clone().add(r, c), s = n - i < 0, u = e.clone().add(r + (s ? -1 : 1), c);
					return +(-(r + (n - i) / (s ? i - u : u - i)) || 0);
				},
				a: function(t) {
					return t < 0 ? Math.ceil(t) || 0 : Math.floor(t);
				},
				p: function(t) {
					return {
						M: c,
						y: h,
						w: o,
						d: a,
						D: d,
						h: u,
						m: s,
						s: i,
						ms: r,
						Q: f
					}[t] || String(t || "").toLowerCase().replace(/s$/, "");
				},
				u: function(t) {
					return void 0 === t;
				}
			}, g = "en", D = {};
			D[g] = M;
			var p = "$isDayjsObject", S = function(t) {
				return t instanceof _ || !(!t || !t[p]);
			}, w = function t(e, n, r) {
				var i;
				if (!e) return g;
				if ("string" == typeof e) {
					var s = e.toLowerCase();
					D[s] && (i = s), n && (D[s] = n, i = s);
					var u = e.split("-");
					if (!i && u.length > 1) return t(u[0]);
				} else {
					var a = e.name;
					D[a] = e, i = a;
				}
				return !r && i && (g = i), i || !r && g;
			}, O = function(t, e) {
				if (S(t)) return t.clone();
				var n = "object" == typeof e ? e : {};
				return n.date = t, n.args = arguments, new _(n);
			}, b = v;
			b.l = w, b.i = S, b.w = function(t, e) {
				return O(t, {
					locale: e.$L,
					utc: e.$u,
					x: e.$x,
					$offset: e.$offset
				});
			};
			var _ = function() {
				function M(t) {
					this.$L = w(t.locale, null, !0), this.parse(t), this.$x = this.$x || t.x || {}, this[p] = !0;
				}
				var m = M.prototype;
				return m.parse = function(t) {
					this.$d = function(t) {
						var e = t.date, n = t.utc;
						if (null === e) return /* @__PURE__ */ new Date(NaN);
						if (b.u(e)) return /* @__PURE__ */ new Date();
						if (e instanceof Date) return new Date(e);
						if ("string" == typeof e && !/Z$/i.test(e)) {
							var r = e.match($);
							if (r) {
								var i = r[2] - 1 || 0, s = (r[7] || "0").substring(0, 3);
								return n ? new Date(Date.UTC(r[1], i, r[3] || 1, r[4] || 0, r[5] || 0, r[6] || 0, s)) : new Date(r[1], i, r[3] || 1, r[4] || 0, r[5] || 0, r[6] || 0, s);
							}
						}
						return new Date(e);
					}(t), this.init();
				}, m.init = function() {
					var t = this.$d;
					this.$y = t.getFullYear(), this.$M = t.getMonth(), this.$D = t.getDate(), this.$W = t.getDay(), this.$H = t.getHours(), this.$m = t.getMinutes(), this.$s = t.getSeconds(), this.$ms = t.getMilliseconds();
				}, m.$utils = function() {
					return b;
				}, m.isValid = function() {
					return !(this.$d.toString() === l);
				}, m.isSame = function(t, e) {
					var n = O(t);
					return this.startOf(e) <= n && n <= this.endOf(e);
				}, m.isAfter = function(t, e) {
					return O(t) < this.startOf(e);
				}, m.isBefore = function(t, e) {
					return this.endOf(e) < O(t);
				}, m.$g = function(t, e, n) {
					return b.u(t) ? this[e] : this.set(n, t);
				}, m.unix = function() {
					return Math.floor(this.valueOf() / 1e3);
				}, m.valueOf = function() {
					return this.$d.getTime();
				}, m.startOf = function(t, e) {
					var n = this, r = !!b.u(e) || e, f = b.p(t), l = function(t, e) {
						var i = b.w(n.$u ? Date.UTC(n.$y, e, t) : new Date(n.$y, e, t), n);
						return r ? i : i.endOf(a);
					}, $ = function(t, e) {
						return b.w(n.toDate()[t].apply(n.toDate("s"), (r ? [
							0,
							0,
							0,
							0
						] : [
							23,
							59,
							59,
							999
						]).slice(e)), n);
					}, y = this.$W, M = this.$M, m = this.$D, v = "set" + (this.$u ? "UTC" : "");
					switch (f) {
						case h: return r ? l(1, 0) : l(31, 11);
						case c: return r ? l(1, M) : l(0, M + 1);
						case o:
							var g = this.$locale().weekStart || 0, D = (y < g ? y + 7 : y) - g;
							return l(r ? m - D : m + (6 - D), M);
						case a:
						case d: return $(v + "Hours", 0);
						case u: return $(v + "Minutes", 1);
						case s: return $(v + "Seconds", 2);
						case i: return $(v + "Milliseconds", 3);
						default: return this.clone();
					}
				}, m.endOf = function(t) {
					return this.startOf(t, !1);
				}, m.$set = function(t, e) {
					var n, o = b.p(t), f = "set" + (this.$u ? "UTC" : ""), l = (n = {}, n[a] = f + "Date", n[d] = f + "Date", n[c] = f + "Month", n[h] = f + "FullYear", n[u] = f + "Hours", n[s] = f + "Minutes", n[i] = f + "Seconds", n[r] = f + "Milliseconds", n)[o], $ = o === a ? this.$D + (e - this.$W) : e;
					if (o === c || o === h) {
						var y = this.clone().set(d, 1);
						y.$d[l]($), y.init(), this.$d = y.set(d, Math.min(this.$D, y.daysInMonth())).$d;
					} else l && this.$d[l]($);
					return this.init(), this;
				}, m.set = function(t, e) {
					return this.clone().$set(t, e);
				}, m.get = function(t) {
					return this[b.p(t)]();
				}, m.add = function(r, f) {
					var d, l = this;
					r = Number(r);
					var $ = b.p(f), y = function(t) {
						var e = O(l);
						return b.w(e.date(e.date() + Math.round(t * r)), l);
					};
					if ($ === c) return this.set(c, this.$M + r);
					if ($ === h) return this.set(h, this.$y + r);
					if ($ === a) return y(1);
					if ($ === o) return y(7);
					var M = (d = {}, d[s] = e, d[u] = n, d[i] = t, d)[$] || 1, m = this.$d.getTime() + r * M;
					return b.w(m, this);
				}, m.subtract = function(t, e) {
					return this.add(-1 * t, e);
				}, m.format = function(t) {
					var e = this, n = this.$locale();
					if (!this.isValid()) return n.invalidDate || l;
					var r = t || "YYYY-MM-DDTHH:mm:ssZ", i = b.z(this), s = this.$H, u = this.$m, a = this.$M, o = n.weekdays, c = n.months, f = n.meridiem, h = function(t, n, i, s) {
						return t && (t[n] || t(e, r)) || i[n].slice(0, s);
					}, d = function(t) {
						return b.s(s % 12 || 12, t, "0");
					}, $ = f || function(t, e, n) {
						var r = t < 12 ? "AM" : "PM";
						return n ? r.toLowerCase() : r;
					};
					return r.replace(y, (function(t, r) {
						return r || function(t) {
							switch (t) {
								case "YY": return String(e.$y).slice(-2);
								case "YYYY": return b.s(e.$y, 4, "0");
								case "M": return a + 1;
								case "MM": return b.s(a + 1, 2, "0");
								case "MMM": return h(n.monthsShort, a, c, 3);
								case "MMMM": return h(c, a);
								case "D": return e.$D;
								case "DD": return b.s(e.$D, 2, "0");
								case "d": return String(e.$W);
								case "dd": return h(n.weekdaysMin, e.$W, o, 2);
								case "ddd": return h(n.weekdaysShort, e.$W, o, 3);
								case "dddd": return o[e.$W];
								case "H": return String(s);
								case "HH": return b.s(s, 2, "0");
								case "h": return d(1);
								case "hh": return d(2);
								case "a": return $(s, u, !0);
								case "A": return $(s, u, !1);
								case "m": return String(u);
								case "mm": return b.s(u, 2, "0");
								case "s": return String(e.$s);
								case "ss": return b.s(e.$s, 2, "0");
								case "SSS": return b.s(e.$ms, 3, "0");
								case "Z": return i;
							}
							return null;
						}(t) || i.replace(":", "");
					}));
				}, m.utcOffset = function() {
					return 15 * -Math.round(this.$d.getTimezoneOffset() / 15);
				}, m.diff = function(r, d, l) {
					var $, y = this, M = b.p(d), m = O(r), v = (m.utcOffset() - this.utcOffset()) * e, g = this - m, D = function() {
						return b.m(y, m);
					};
					switch (M) {
						case h:
							$ = D() / 12;
							break;
						case c:
							$ = D();
							break;
						case f:
							$ = D() / 3;
							break;
						case o:
							$ = (g - v) / 6048e5;
							break;
						case a:
							$ = (g - v) / 864e5;
							break;
						case u:
							$ = g / n;
							break;
						case s:
							$ = g / e;
							break;
						case i:
							$ = g / t;
							break;
						default: $ = g;
					}
					return l ? $ : b.a($);
				}, m.daysInMonth = function() {
					return this.endOf(c).$D;
				}, m.$locale = function() {
					return D[this.$L];
				}, m.locale = function(t, e) {
					if (!t) return this.$L;
					var n = this.clone(), r = w(t, e, !0);
					return r && (n.$L = r), n;
				}, m.clone = function() {
					return b.w(this.$d, this);
				}, m.toDate = function() {
					return new Date(this.valueOf());
				}, m.toJSON = function() {
					return this.isValid() ? this.toISOString() : null;
				}, m.toISOString = function() {
					return this.$d.toISOString();
				}, m.toString = function() {
					return this.$d.toUTCString();
				}, M;
			}(), k = _.prototype;
			return O.prototype = k, [
				["$ms", r],
				["$s", i],
				["$m", s],
				["$H", u],
				["$W", a],
				["$M", c],
				["$y", h],
				["$D", d]
			].forEach((function(t) {
				k[t[1]] = function(e) {
					return this.$g(e, t[0], t[1]);
				};
			})), O.extend = function(t, e) {
				return t.$i || (t(e, _, O), t.$i = !0), O;
			}, O.locale = w, O.isDayjs = S, O.unix = function(t) {
				return O(1e3 * t);
			}, O.en = D[g], O.Ls = D, O.p = {}, O;
		}));
	}));
	var require_zh_cn = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		(function(e, _) {
			"object" == typeof exports && "undefined" != typeof module ? module.exports = _(require_dayjs_min()) : "function" == typeof define && define.amd ? define(["dayjs"], _) : (e = "undefined" != typeof globalThis ? globalThis : e || self).dayjs_locale_zh_cn = _(e.dayjs);
		})(exports, (function(e) {
			"use strict";
			function _(e) {
				return e && "object" == typeof e && "default" in e ? e : { default: e };
			}
			var t = _(e), d = {
				name: "zh-cn",
				weekdays: "星期日_星期一_星期二_星期三_星期四_星期五_星期六".split("_"),
				weekdaysShort: "周日_周一_周二_周三_周四_周五_周六".split("_"),
				weekdaysMin: "日_一_二_三_四_五_六".split("_"),
				months: "一月_二月_三月_四月_五月_六月_七月_八月_九月_十月_十一月_十二月".split("_"),
				monthsShort: "1月_2月_3月_4月_5月_6月_7月_8月_9月_10月_11月_12月".split("_"),
				ordinal: function(e, _) {
					return "W" === _ ? e + "周" : e + "日";
				},
				weekStart: 1,
				yearStart: 4,
				formats: {
					LT: "HH:mm",
					LTS: "HH:mm:ss",
					L: "YYYY/MM/DD",
					LL: "YYYY年M月D日",
					LLL: "YYYY年M月D日Ah点mm分",
					LLLL: "YYYY年M月D日ddddAh点mm分",
					l: "YYYY/M/D",
					ll: "YYYY年M月D日",
					lll: "YYYY年M月D日 HH:mm",
					llll: "YYYY年M月D日dddd HH:mm"
				},
				relativeTime: {
					future: "%s内",
					past: "%s前",
					s: "几秒",
					m: "1 分钟",
					mm: "%d 分钟",
					h: "1 小时",
					hh: "%d 小时",
					d: "1 天",
					dd: "%d 天",
					M: "1 个月",
					MM: "%d 个月",
					y: "1 年",
					yy: "%d 年"
				},
				meridiem: function(e, _) {
					var t = 100 * e + _;
					return t < 600 ? "凌晨" : t < 900 ? "早上" : t < 1100 ? "上午" : t < 1300 ? "中午" : t < 1800 ? "下午" : "晚上";
				}
			};
			return t.default.locale(d, null, !0), d;
		}));
	}));
	var import_relativeTime = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
		(function(r, e) {
			"object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (r = "undefined" != typeof globalThis ? globalThis : r || self).dayjs_plugin_relativeTime = e();
		})(exports, (function() {
			"use strict";
			return function(r, e, t) {
				r = r || {};
				var n = e.prototype, o = {
					future: "in %s",
					past: "%s ago",
					s: "a few seconds",
					m: "a minute",
					mm: "%d minutes",
					h: "an hour",
					hh: "%d hours",
					d: "a day",
					dd: "%d days",
					M: "a month",
					MM: "%d months",
					y: "a year",
					yy: "%d years"
				};
				function i(r, e, t, o) {
					return n.fromToBase(r, e, t, o);
				}
				t.en.relativeTime = o, n.fromToBase = function(e, n, i, d, u) {
					for (var f, a, s, l = i.$locale().relativeTime || o, h = r.thresholds || [
						{
							l: "s",
							r: 44,
							d: "second"
						},
						{
							l: "m",
							r: 89
						},
						{
							l: "mm",
							r: 44,
							d: "minute"
						},
						{
							l: "h",
							r: 89
						},
						{
							l: "hh",
							r: 21,
							d: "hour"
						},
						{
							l: "d",
							r: 35
						},
						{
							l: "dd",
							r: 25,
							d: "day"
						},
						{
							l: "M",
							r: 45
						},
						{
							l: "MM",
							r: 10,
							d: "month"
						},
						{
							l: "y",
							r: 17
						},
						{
							l: "yy",
							d: "year"
						}
					], m = h.length, c = 0; c < m; c += 1) {
						var y = h[c];
						y.d && (f = d ? t(e).diff(i, y.d, !0) : i.diff(e, y.d, !0));
						var p = (r.rounding || Math.round)(Math.abs(f));
						if (s = f > 0, p <= y.r || !y.r) {
							p <= 1 && c > 0 && (y = h[c - 1]);
							var v = l[y.l];
							u && (p = u("" + p)), a = "string" == typeof v ? v.replace("%d", p) : v(p, n, y.l, s);
							break;
						}
					}
					if (n) return a;
					var M = s ? l.future : l.past;
					return "function" == typeof M ? M(a) : M.replace("%s", a);
				}, n.to = function(r, e) {
					return i(r, e, this, !0);
				}, n.from = function(r, e) {
					return i(r, e, this);
				};
				var d = function(r) {
					return r.$u ? t.utc() : t();
				};
				n.toNow = function(r) {
					return this.to(d(this), r);
				}, n.fromNow = function(r) {
					return this.from(d(this), r);
				};
			};
		}));
	})))());
	require_zh_cn();
	var import_dayjs_min = /* @__PURE__ */ __toESM(require_dayjs_min());
	const lang_cn_b = {
		ch: "零壹贰叁肆伍陆柒捌玖",
		ch_u: "个拾佰仟万亿",
		ch_f: "负",
		ch_d: "点",
		m_t: "人民币",
		m_z: "整",
		m_u: "元角分"
	};
	const lang_cn_s = {
		ch: "零一二三四五六七八九",
		ch_u: "个十百千万亿",
		ch_f: "负",
		ch_d: "点"
	};
	var REG_NUMBER = /^([+-])?0*(\d+)(\.(\d+))?$/;
	var REG_E = /^([+-])?0*(\d+)(\.(\d+))?e(([+-])?(\d+))$/i;
	/**
	* 科学计数法转十进制
	*
	* @param {string} num 科学记数法字符串
	* @returns string
	*/
	function e2ten(num) {
		var result = REG_E.exec(num.toString());
		if (!result) return num;
		var zs = result[2], xs = result[4] || "", e = result[5] ? +result[5] : 0;
		if (e > 0) {
			var _zs = xs.substr(0, e);
			_zs = _zs.length < e ? _zs + new Array(e - _zs.length + 1).join("0") : _zs;
			xs = xs.substr(e);
			zs += _zs;
		} else {
			e = -e;
			var s_start = zs.length - e;
			s_start = s_start < 0 ? 0 : s_start;
			var _xs = zs.substr(s_start, e);
			_xs = _xs.length < e ? new Array(e - _xs.length + 1).join("0") + _xs : _xs;
			zs = zs.substring(0, s_start);
			xs = _xs + xs;
		}
		zs = zs == "" ? "0" : zs;
		return (result[1] == "-" ? "-" : "") + zs + (xs ? "." + xs : "");
	}
	/**
	* 分析数字字符串
	*
	* @param {string} num NumberString
	* @returns object
	*/
	function getNumbResult(num) {
		var result = REG_NUMBER.exec(num.toString());
		if (!result && REG_E.test(num.toString())) result = REG_NUMBER.exec(e2ten(num.toString()));
		if (result) return {
			int: result[2],
			decimal: result[4],
			minus: result[1] == "-",
			num: result.slice(1, 3).join("")
		};
		return null;
	}
	/**
	* 数组归一 (按索引覆盖合并数组,并清空被合并的数组)
	*
	* @param {array} baseArray 基础数组
	* @param {...array} array1
	* @returns array
	*/
	function centerArray(baseArray, array1, array2) {
		baseArray.splice.apply(baseArray, [0, array1.length].concat(array1.splice(0, array1.length)));
		if (arguments.length > 2) {
			var r = [].slice.call(arguments, 2);
			r.unshift(baseArray);
			centerArray.apply(null, r);
		}
		return baseArray;
	}
	/**
	* 检查对像属性 (非原型链)
	*
	* @param {object} obj
	* @param {string} key
	* @returns
	*/
	function hasAttr(obj, key) {
		return Object.prototype.hasOwnProperty.call(obj, key);
	}
	/**
	* 扩展对像(浅复制)
	*
	* @param {object} obj
	* @param {object} obj1
	* @returns
	*/
	function extend(...args) {
		var name, target = arguments[0] || {};
		var objs = Array.prototype.slice.call(arguments, 1);
		for (var i = 0; i < objs.length; i++) {
			var _obj = objs[i];
			for (name in _obj) if (hasAttr(_obj, name)) target[name] = _obj[name];
		}
		return target;
	}
	/**
	* 获取真实数位
	*
	* @param {number} index 中文单位的索引
	*/
	function getDigit(index) {
		return index >= 5 ? (index - 4) * 4 + 4 : index;
	}
	/**
	* 往数组头部插入0
	*
	* @param {array} arr
	* @param {number} n
	*/
	function unshiftZero(arr, n) {
		if (n == null) n = 1;
		if (n <= 0) return;
		for (; n--;) arr.unshift(0);
	}
	/**
	* 清理多余"零"
	*
	* @param {any} str
	* @param {any} zero "零"字符
	* @param {any} type 清理模式 ^ - 开头, $ - 结尾, nto1 - 多个连续变一个
	* @returns
	*/
	function clearZero(str, zero, type) {
		if (str == null) return "";
		var reg0 = ~"*.?+$^[](){}|\\/".indexOf(zero) ? "\\" + zero : zero;
		var arg_s = new RegExp("^" + reg0 + "+"), arg_e = new RegExp(reg0 + "+$"), arg_d = new RegExp(reg0 + "{2}", "g");
		str = str.toString();
		if (type == "^") str = str.replace(arg_s, "");
		if (!type || type == "$") str = str.replace(arg_e, "");
		if (!type || type == "nto1") str = str.replace(arg_d, zero);
		return str;
	}
	/**
	* 阿拉伯数字转中文数字
	*
	* @param {String} num 阿拉伯数字/字符串 , 科学记数法字符串
	* @param {Object} opration 转换配置
	*                          {
	*                              ww: {万万化单位 | false}
	*                              tenMin: {十的口语化 | false}
	*                          }
	* @returns String
	*/
	function cl(num, options = {}) {
		const result = getNumbResult(num);
		if (!result) return String(num);
		const { ch = "", ch_u = "", ch_f = "", ch_d = "." } = options;
		var n0 = ch.charAt(0);
		var _int = result.int;
		var _decimal = result.decimal;
		var _minus = result.minus;
		var int = "";
		var decimal = "";
		var minus = _minus ? ch_f : "";
		const encodeInt = function encodeInt(_int, _m, _dg) {
			_int = getNumbResult(_int).int;
			var int = "";
			var tenm = arguments.length > 1 ? arguments[1] : options.tenMin;
			var _length = _int.length;
			if (_length === 1) return ch.charAt(+_int);
			if (_length <= 4) for (var i = 0, n = _length; n--;) {
				var _num = +_int.charAt(i);
				int += tenm && _length == 2 && i == 0 && _num == 1 ? "" : ch.charAt(_num);
				int += _num && n ? ch_u.charAt(n) : "";
				i++;
			}
			else {
				var d = _int.length / 4 >> 0;
				var y = _int.length % 4;
				while (y == 0 || !ch_u.charAt(3 + d)) {
					y += 4;
					d--;
				}
				var _maxLeft = _int.substr(0, y), _other = _int.substr(y);
				int = encodeInt(_maxLeft, tenm) + ch_u.charAt(3 + d) + (_other.charAt(0) == "0" ? n0 : "") + encodeInt(_other, _other.length > 4 ? tenm : false);
			}
			int = clearZero(int, n0);
			return int;
		};
		if (_decimal) {
			_decimal = clearZero(_decimal, "0", "$");
			for (var x = 0; x < _decimal.length; x++) decimal += ch.charAt(+_decimal.charAt(x));
			decimal = decimal ? ch_d + decimal : "";
		}
		int = encodeInt(_int);
		if (options.ww && ch_u.length > 5) {
			var dw_w = ch_u.charAt(4), dw_y = ch_u.charAt(5);
			var lasty = int.lastIndexOf(dw_y);
			if (~lasty) int = int.substring(0, lasty).replace(new RegExp(dw_y, "g"), dw_w + dw_w) + int.substring(lasty);
		}
		return minus + int + decimal;
	}
	function uncl(cnnumb, options = {}) {
		cnnumb = cnnumb.toString();
		const { ch = "", ch_d = "", ch_f = "", ch_u = "" } = options;
		var result = cnnumb.split(ch_d);
		var _int = result[0].replace(ch_f, ""), _decimal = result[1], _minus = !!~result[0].indexOf(ch_f);
		var dw_s = ch_u.charAt(1);
		var dw_w = ch_u.charAt(4);
		var dw_y = ch_u.charAt(5);
		_int = _int.replace(new RegExp(dw_w + "{2}", "g"), dw_y);
		var cnarr = _int.split("");
		var dw = 0, maxdw = 0;
		var rnum_a = [];
		var num_a = [];
		var _num_a = [];
		for (var i = 0; i < cnarr.length; i++) {
			var chr = cnarr[i];
			var n = 0, u = 0;
			if (~(n = ch.indexOf(chr))) {
				if (n > 0) _num_a.unshift(n);
			} else if (~(u = ch_u.indexOf(chr))) {
				var digit = getDigit(u);
				if (dw > u) {
					unshiftZero(_num_a, digit);
					centerArray(num_a, _num_a);
				} else if (u >= maxdw) {
					if (i == 0) _num_a = [1];
					centerArray(rnum_a, num_a, _num_a);
					if (rnum_a.length > 0) unshiftZero(rnum_a, digit);
					maxdw = u;
				} else {
					if (_num_a.length == 0 && dw_s == chr) _num_a = [1];
					centerArray(num_a, _num_a);
					unshiftZero(num_a, getDigit(u));
					dw = u;
				}
			}
		}
		centerArray(rnum_a, num_a, _num_a).reverse();
		if (rnum_a.length == 0) rnum_a.push(0);
		var decimal = 0;
		if (_decimal) {
			rnum_a.push(".");
			decimal = "0.";
			for (var i = 0; i < _decimal.length; i++) {
				decimal += ch.indexOf(_decimal.charAt(i));
				rnum_a.push(ch.indexOf(_decimal.charAt(i)));
			}
			decimal = +decimal;
		}
		if (_minus) rnum_a.unshift("-");
		return parseFloat(rnum_a.join(""));
	}
	/**
	* 中文数字转阿拉伯数字
	*
	* @param {string} cnnumb 中文数字字符串
	* @returns Number
	*/
	const cn = {
		encodeS(num, options = {}) {
			return cl(num, extend({
				ww: true,
				...lang_cn_s
			}, options));
		},
		encodeB(num, options = {}) {
			return cl(num, extend({
				ww: true,
				...lang_cn_b
			}, options));
		},
		decodeS(num, options = {}) {
			return uncl(num, extend({
				ww: true,
				...lang_cn_s
			}, options));
		}
	};
	function toNumber(v, default_v) {
		const vv = Number(v);
		if (Number.isNaN(vv)) {
			if (default_v !== void 0) return default_v;
			return null;
		}
		return vv;
	}
	function inRange(v, [a, b]) {
		return v >= a && v <= b;
	}
	function downloadFile(url, filename) {
		const link = document.createElement("a");
		link.href = url;
		link.download = filename;
		link.style.display = "none";
		document.body.appendChild(link);
		link.click();
		document.body.removeChild(link);
	}
	function debounce(wait, func) {
		let timeoutId;
		return function debounced(...args) {
			if (timeoutId) clearTimeout(timeoutId);
			timeoutId = setTimeout(() => {
				func(...args);
				timeoutId = null;
			}, wait);
		};
	}
	function throttle(delay, func) {
		let lastTime = 0;
		return function(...args) {
			const currentTime = Date.now();
			if (currentTime - lastTime >= delay) {
				lastTime = currentTime;
				return func(...args);
			}
		};
	}
	/**
	* A simple implementation of qs.parse and qs.stringify
	* Handles basic query string parsing/stringifying
	*/
	function qs_parse(str, options = {}) {
		if (typeof str !== "string") return {};
		let queryString = str.trim();
		if (options.ignoreQueryPrefix) {
			if (queryString.length > 0 && queryString.charAt(0) === "?") queryString = queryString.substring(1);
		}
		if (queryString === "") return {};
		const pairs = queryString.split("&");
		const result = {};
		for (const pair of pairs) {
			if (!pair) continue;
			const parts = pair.split("=");
			const key = decodeURIComponent(parts[0]);
			result[key] = parts.length > 1 ? decodeURIComponent(parts.slice(1).join("=")) : "";
		}
		return result;
	}
	function qs_stringify(obj) {
		if (!obj || typeof obj !== "object") return "";
		const keys = Object.keys(obj);
		if (keys.length === 0) return "";
		return keys.map((key) => {
			const value = obj[key];
			if (value === void 0) return "";
			if (value === null) return encodeURIComponent(key) + "=";
			return encodeURIComponent(key) + "=" + encodeURIComponent(String(value));
		}).filter((item) => item !== "").join("&");
	}
	/** 解析一段 json 字符串 */
	function parseJSONStr(json) {
		try {
			if (json[0] !== "{" && json[0] !== "[") return _timeless_base.Result.Err("不是合法的 json");
			const d = JSON.parse(json);
			return _timeless_base.Result.Ok(d);
		} catch (err) {
			const e = err;
			return _timeless_base.Result.Err(e);
		}
	}
	function loadImage(data) {
		return new Promise((resolve) => {
			const img = new Image();
			img.onload = () => {
				resolve(_timeless_base.Result.Ok(img));
			};
			img.onerror = (msg) => {
				resolve(_timeless_base.Result.Err(msg));
			};
			img.src = data;
		});
	}
	function blobToArrayBuffer(blob) {
		return new Promise((resolve) => {
			const reader = new FileReader();
			reader.onload = () => {
				const buffer = reader.result;
				resolve(buffer);
			};
			reader.readAsArrayBuffer(blob);
		});
	}
	function readFileAsURL(file) {
		return new Promise((resolve) => {
			const reader = new FileReader();
			reader.onload = (e) => {
				if (e.target === null) return resolve(_timeless_base.Result.Err("read failed"));
				resolve(_timeless_base.Result.Ok(reader.result));
			};
			reader.readAsDataURL(file);
		});
	}
	function readFileAsArrayBuffer(file) {
		return new Promise((resolve) => {
			const reader = new FileReader();
			reader.onload = (e) => {
				if (e.target === null) return resolve(_timeless_base.Result.Err("read failed"));
				const buffer = e.target.result;
				return resolve(_timeless_base.Result.Ok(buffer));
			};
			reader.readAsArrayBuffer(file);
		});
	}
	import_dayjs_min.default.extend(import_relativeTime.default);
	import_dayjs_min.default.locale("zh-cn");
	function toFixed(v, n = 2) {
		return Number(Number(v).toFixed(n));
	}
	function uidFactory() {
		let _uid = 0;
		return function uid() {
			_uid += 1;
			return _uid;
		};
	}
	var defaultRandomAlphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
	/**
	* 返回一个指定长度的随机字符串
	* @param length
	* @returns
	*/
	function random_key(length) {
		return random_string_with_alphabet(length, defaultRandomAlphabet);
	}
	function random_string_with_alphabet(length, alphabet) {
		let b = new Array(length);
		let max = alphabet.length;
		for (let i = 0; i < b.length; i++) b[i] = alphabet[Math.floor(Math.random() * max)];
		return b.join("");
	}
	function padding_zero(str) {
		if (String(str).length === 1) return `0${str}`;
		return String(str);
	}
	function remove_str(filename, index = 0, length) {
		return filename.slice(0, index) + filename.slice(index + length);
	}
	/**
	* 阿拉伯数字转中文数字
	* @param num
	* @returns
	*/
	function num_to_chinese(num) {
		return cn.encodeS(num);
	}
	function chinese_num_to_num(str) {
		return cn.decodeS(str);
	}
	function update_arr_item(arr, index, v2) {
		if (index === -1) return [...arr];
		return [
			...arr.slice(0, index),
			v2,
			...arr.slice(index + 1)
		];
	}
	function remove_arr_item(arr, index) {
		if (index === -1) return [...arr];
		return [...arr.slice(0, index), ...arr.slice(index + 1)];
	}
	function sleep(ms) {
		return new Promise((resolve) => setTimeout(resolve, ms));
	}
	exports.blobToArrayBuffer = blobToArrayBuffer;
	exports.chinese_num_to_num = chinese_num_to_num;
	exports.debounce = debounce;
	exports.downloadFile = downloadFile;
	exports.inRange = inRange;
	exports.loadImage = loadImage;
	exports.num_to_chinese = num_to_chinese;
	exports.padding_zero = padding_zero;
	exports.parseJSONStr = parseJSONStr;
	exports.qs_parse = qs_parse;
	exports.qs_stringify = qs_stringify;
	exports.random_key = random_key;
	exports.readFileAsArrayBuffer = readFileAsArrayBuffer;
	exports.readFileAsURL = readFileAsURL;
	exports.remove_arr_item = remove_arr_item;
	exports.remove_str = remove_str;
	exports.sleep = sleep;
	exports.throttle = throttle;
	exports.toFixed = toFixed;
	exports.toNumber = toNumber;
	exports.uidFactory = uidFactory;
	exports.update_arr_item = update_arr_item;
});

//# sourceMappingURL=timeless.utils.umd.min.js.map