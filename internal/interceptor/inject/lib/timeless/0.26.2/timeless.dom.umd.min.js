(function(global, factory) {
	typeof exports === "object" && typeof module !== "undefined" ? factory(exports, require("@timeless/timeless")) : typeof define === "function" && define.amd ? define(["exports", "@timeless/timeless"], factory) : (global = typeof globalThis !== "undefined" ? globalThis : global || self, factory((global.Timeless = global.Timeless || {}, global.Timeless.DOM = {}), global.Timeless));
})(this, function(exports, _timeless_timeless) {
	Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
	function DOMStyle(props) {
		const t = "style";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElement("style");
				$elm.type = "text/css";
				if (elm.state.content) $elm.textContent = elm.state.content;
				box$.methods.set$elm($elm);
				document.head.appendChild($elm);
				return document.createDocumentFragment();
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
			}
		};
	}
	function viewStyleToCssText(style) {
		if (!style) return "";
		if (typeof style === "string") return style;
		const parts = [];
		const keys = Object.keys(style);
		for (let i = 0; i < keys.length; i += 1) {
			const k = keys[i];
			const vv = style[k];
			const v = (0, _timeless_timeless.isRef)(vv) ? vv.value : vv;
			if (v === void 0 || v === null || v === false) continue;
			parts.push(`${k}: ${String(v)}`);
		}
		return parts.join("; ");
	}
	var _batch_mode = false;
	var _pending_mounted = [];
	var _raf_scheduled = false;
	function HostElement(props) {
		let $elm = props.$elm;
		/** 宿主平台的子节点列表 */
		let $children = [];
		/** 对宿主平台抽象的子节点列表 */
		let child_nodes = [];
		/** Timeless 子列表 */
		let child_elements = [];
		let _events = null;
		const methods = {
			set$elm(elm) {
				$elm = elm;
			},
			startBatch() {
				_batch_mode = true;
			},
			endBatch() {
				_batch_mode = false;
				if (_pending_mounted.length > 0 && !_raf_scheduled) {
					_raf_scheduled = true;
					requestAnimationFrame(() => {
						_raf_scheduled = false;
						const cbs = _pending_mounted;
						_pending_mounted = [];
						for (const cb of cbs) cb();
					});
				}
			},
			get$elm() {
				return $elm;
			},
			set$childrne(v) {
				$children = v;
			},
			setchildrenelement(v) {
				child_elements = v;
			},
			setchildnode(v) {
				child_nodes = v;
			},
			setStyle(style, opt = {}) {
				if (!$elm || $elm instanceof Text) return;
				const cssText = viewStyleToCssText(style);
				if (!opt.initial) if (cssText) $elm.style.cssText = cssText;
				else $elm.removeAttribute("style");
				else if (cssText) $elm.style.cssText = cssText;
			},
			setStyleSet(styleSet, opt = {}) {
				if (!$elm || $elm instanceof Text || !styleSet) return;
				if (!opt.initial) if (styleSet.length === 0) $elm.removeAttribute("class");
				else $elm.setAttribute("class", styleSet.join(" "));
				else if (styleSet.length !== 0) $elm.setAttribute("class", styleSet.join(" "));
			},
			setStyleValue(key, value) {
				if (!$elm || $elm instanceof Text) return;
				$elm.style[key] = value;
			},
			setAttribute(key, value) {
				if (!$elm || $elm instanceof Text) return;
				$elm.setAttribute(key, value);
			},
			removeAttribute(key) {
				if (!$elm || $elm instanceof Text) return;
				$elm.removeAttribute(key);
			},
			getBoundingClientRect() {
				if (!$elm || !($elm instanceof HTMLElement)) return {
					x: 0,
					y: 0,
					width: 0,
					height: 0,
					top: 0,
					left: 0,
					right: 0,
					bottom: 0
				};
				return $elm.getBoundingClientRect();
			},
			blur() {
				if (!$elm || $elm instanceof Text) return;
				$elm.blur();
			},
			focus() {
				if (!$elm || $elm instanceof Text) return;
				$elm.focus();
			},
			addEventListener(type, handler, options) {
				if (!$elm || $elm instanceof Text) return;
				$elm.addEventListener(type, handler, options);
				return function() {
					if (!$elm || $elm instanceof Text) return;
					$elm.removeEventListener(type, handler, options);
				};
			},
			removeEventListener(type, handler, options) {
				if (!$elm || $elm instanceof Text) return;
				$elm.removeEventListener(type, handler, options);
			},
			setupEventListener(events) {
				if (!events || !$elm || $elm instanceof Text) return;
				if (events.onClick) $elm.addEventListener("click", events.onClick);
				if (events.onDoubleClick) $elm.addEventListener("dblclick", events.onDoubleClick);
				if (events.onMouseDown) $elm.addEventListener("mousedown", events.onMouseDown);
				if (events.onMouseUp) $elm.addEventListener("mouseup", events.onMouseUp);
				if (events.onPointerDown) $elm.addEventListener("pointerdown", events.onPointerDown);
				if (events.onPointerUp) $elm.addEventListener("pointerup", events.onPointerUp);
				if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
				if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				if (events.onContextMenu) $elm.addEventListener("contextmenu", events.onContextMenu);
				if (events.onMouseEnter) $elm.addEventListener("mouseenter", events.onMouseEnter);
				if (events.onMouseLeave) $elm.addEventListener("mouseleave", events.onMouseLeave);
				if (events.onDragStart) $elm.addEventListener("dragstart", events.onDragStart);
				if (events.onDrag) $elm.addEventListener("drag", events.onDrag);
				if (events.onDragEnd) $elm.addEventListener("dragend", events.onDragEnd);
				if (events.onDragEnter) $elm.addEventListener("dragenter", events.onDragEnter);
				if (events.onDragOver) $elm.addEventListener("dragover", events.onDragOver);
				if (events.onDragLeave) $elm.addEventListener("dragleave", events.onDragLeave);
				if (events.onDrop) $elm.addEventListener("drop", events.onDrop);
				if (events.onAnimationEnd) $elm.addEventListener("animationend", events.onAnimationEnd);
				if (events.onWheel) $elm.addEventListener("wheel", events.onWheel);
			},
			teardownEventListener(events) {
				if (!events || !$elm || $elm instanceof Text) return;
				_events = events;
				if (events.onClick) $elm.removeEventListener("click", events.onClick);
				if (events.onDoubleClick) $elm.removeEventListener("dblclick", events.onDoubleClick);
				if (events.onMouseDown) $elm.removeEventListener("mousedown", events.onMouseDown);
				if (events.onMouseUp) $elm.removeEventListener("mouseup", events.onMouseUp);
				if (events.onPointerDown) $elm.removeEventListener("pointerdown", events.onPointerDown);
				if (events.onFocus) $elm.removeEventListener("focus", events.onFocus);
				if (events.onBlur) $elm.removeEventListener("blur", events.onBlur);
				if (events.onKeyDown) $elm.removeEventListener("keydown", events.onKeyDown);
				if (events.onContextMenu) $elm.removeEventListener("contextmenu", events.onContextMenu);
				if (events.onMouseEnter) $elm.removeEventListener("mouseenter", events.onMouseEnter);
				if (events.onMouseLeave) $elm.removeEventListener("mouseleave", events.onMouseLeave);
				if (events.onDragStart) $elm.removeEventListener("dragstart", events.onDragStart);
				if (events.onDrag) $elm.removeEventListener("drag", events.onDrag);
				if (events.onDragEnd) $elm.removeEventListener("dragend", events.onDragEnd);
				if (events.onDragEnter) $elm.removeEventListener("dragenter", events.onDragEnter);
				if (events.onDragOver) $elm.removeEventListener("dragover", events.onDragOver);
				if (events.onDragLeave) $elm.removeEventListener("dragleave", events.onDragLeave);
				if (events.onDrop) $elm.removeEventListener("drop", events.onDrop);
				if (events.onAnimationEnd) $elm.removeEventListener("animationend", events.onAnimationEnd);
			},
			applyState(state, opt = {}) {
				if (!state) return;
				if (state.style) methods.setStyle(state.style, opt);
				if (state.styleSet) methods.setStyleSet(state.styleSet, opt);
				const attrs = state.attributes;
				if (attrs) for (const [key, value] of Object.entries(attrs)) if (value !== void 0) methods.setAttribute(key, String(value));
				else methods.removeAttribute(key);
				const dataset = state.dataset;
				if (dataset) for (const [key, value] of Object.entries(dataset)) {
					const k = `data-${key}`;
					if (value !== void 0) methods.setAttribute(k, String(value));
					else methods.removeAttribute(k);
				}
			},
			render(children = []) {
				child_elements = [];
				const $fragment = document.createDocumentFragment();
				for (const child of children) if ((0, _timeless_timeless.isElement)(child)) {
					const child$ = props.build(child);
					const $child = child$.render(child);
					child_elements.push(child);
					child_nodes.push(child$);
					if ($child) {
						$children.push($child);
						$fragment.appendChild($child);
					}
				}
				return $fragment;
			},
			handleElementsMounted() {
				for (const child of child_elements) if (child && child.onMounted) child.onMounted({ target: child.$elm });
			},
			handleElementUnmounted() {
				for (const child of child_elements) if (child && child.onUnmounted) child.onUnmounted();
			},
			get$children() {
				return $children;
			},
			getChildren() {
				return child_nodes;
			},
			buildChildren(children) {
				const child_elements = [];
				const child_host_nodes = [];
				const child_nodes = [];
				const $fragment = document.createDocumentFragment();
				if (!children) return {
					$fragment,
					child_elements,
					child_host_nodes,
					child_nodes
				};
				for (let child of children) {
					if (!child) continue;
					if ((0, _timeless_timeless.isElement)(child)) {
						child_elements.push(child);
						const child$ = props.build(child);
						if (!child$) continue;
						child_nodes.push(child$);
						const $child = child$.render(child);
						if ($child) {
							child_host_nodes.push($child);
							$fragment.appendChild($child);
						}
					}
				}
				return {
					$fragment,
					child_elements,
					child_host_nodes,
					child_nodes
				};
			},
			insertChildren(children, opt) {
				const r = methods.buildChildren(children);
				const $parent = opt?.$parent || methods.getParent();
				if ($parent) if ($elm && $elm instanceof Text) $parent.insertBefore(r.$fragment, $elm);
				else $parent.appendChild(r.$fragment);
				child_elements = r.child_elements;
				$children = r.child_host_nodes;
				child_nodes = r.child_nodes;
				if (_batch_mode) _pending_mounted.push(() => methods.handleElementsMounted());
				else setTimeout(() => methods.handleElementsMounted(), 0);
			},
			removeChildren(extra) {
				const $parent = extra?.$parent || methods.getParent();
				if ($children.length === 0 && child_nodes.length === 0) return;
				for (let i = 0; i < child_nodes.length; i += 1) {
					const child_node = child_nodes[i];
					if (child_node) child_node.removeChildren();
				}
				if ($parent) {
					for (const $child of $children) if ($child && $child.parentNode === $parent) $parent.removeChild($child);
				}
				$children.length = 0;
				child_nodes.length = 0;
				methods.teardownEventListener(_events);
				setTimeout(() => {
					for (const child of child_elements) if (child && child.onUnmounted) child.onUnmounted();
					child_elements.length = 0;
				}, 0);
			},
			insert(idx, children, extra) {
				if (children.length === 0) return;
				const $parent = extra?.$parent || methods.getParent();
				if (!$parent) return;
				/**
				* 这里的 $elm 其实是 $anchor，一个 文本节点用于瞄定结尾位置
				* 比如 [Show({}), For({}), View({})] 这种场景，For 组件插入元素是在自己的「范围」内
				* 这个范围，无法靠 $children 来确定
				*/
				const $reference = $children[idx];
				const inserted_elements = [];
				const inserted_child = [];
				const inserted_host_nodes = [];
				const $fragment = document.createDocumentFragment();
				for (const child of children) if (child) {
					const child$ = props.build(child);
					inserted_child.push(child$);
					const $child = child$.render(child);
					if ($child) {
						inserted_host_nodes.push($child);
						inserted_elements.push(child);
						$fragment.appendChild($child);
					}
				}
				if ($reference) $parent.insertBefore($fragment, $reference);
				else if ($elm) $parent.insertBefore($fragment, $elm);
				else $parent.appendChild($fragment);
				$children.splice(idx, 0, ...inserted_host_nodes);
				child_elements.splice(idx, 0, ...inserted_elements);
				child_nodes.splice(idx, 0, ...inserted_child);
				if (_batch_mode) {
					const elements = [...inserted_elements];
					_pending_mounted.push(() => {
						for (const child of elements) if (child.onMounted) child.onMounted({ target: child.$elm });
					});
				} else for (const child of inserted_elements) if (child.onMounted) child.onMounted({ target: child.$elm });
			},
			remove(idx, count, extra) {
				if (count === 0) return;
				if (!(extra?.$parent || methods.getParent())) {
					console.warn("remove parent not found");
					return;
				}
				const $fragment = document.createDocumentFragment();
				const removed_elements = [];
				for (let i = 0; i < count; i++) {
					const $child = $children[idx + i];
					if ($child) {
						$fragment.appendChild($child);
						const child_elm = child_elements[idx + i];
						if (child_elm) removed_elements.push(child_elm);
					}
				}
				$children.splice(idx, count);
				child_elements.splice(idx, count);
				child_nodes.splice(idx, count);
				setTimeout(() => {
					for (const child of removed_elements) if (child && child.onUnmounted) child.onUnmounted();
				}, 0);
			},
			move(from, to) {
				const $parent = methods.getParent();
				if (!$parent) {
					console.warn("move parent not found");
					return;
				}
				const $from = $children[from];
				if (!$from) {
					console.warn("move node not found from", from);
					return;
				}
				$children.splice(from, 1);
				$children.splice(to, 0, $from);
				const $to = $children[to + 1] || null;
				$parent.insertBefore($from, $to);
			},
			refresh(data, extra) {
				const { added = [], removed = [], moved = [] } = data;
				if (added.length === 0 && removed.length === 0 && moved.length === 0) return;
				if (!(extra?.$parent || methods.getParent())) {
					console.warn("refresh parent not found");
					return;
				}
				const sorted_removed = [...removed].sort((a, b) => b.idx - a.idx);
				const removed_elements = [];
				const removed_childs = [];
				const removed_child_nodes = [];
				const fragment = document.createDocumentFragment();
				for (const { idx, count } of sorted_removed) for (let i = 0; i < count; i++) {
					const $child = $children[idx + i];
					if ($child) fragment.appendChild($child);
				}
				for (const { idx, count } of sorted_removed) {
					const childs = $children.splice(idx, count);
					removed_childs.push(...childs);
					const childs_elm = child_elements.splice(idx, count);
					removed_elements.push(...childs_elm);
					const childs_node = child_nodes.splice(idx, count);
					removed_child_nodes.push(...childs_node);
				}
				for (const child of removed_elements) if (child && child.onUnmounted) child.onUnmounted();
				if (moved.length > 0) {
					const removedIdxs = removed.map((r) => r.idx).sort((a, b) => a - b);
					const entries = moved.map(({ from, to }) => {
						let shift = 0;
						for (const ri of removedIdxs) if (ri < from) shift++;
						else break;
						return {
							$node: $children[from - shift],
							to
						};
					}).sort((a, b) => a.to - b.to);
					for (const { $node, to } of entries) {
						if (!$node) continue;
						const currentFrom = $children.indexOf($node);
						if (currentFrom !== -1 && currentFrom !== to) methods.move(currentFrom, to);
					}
				}
				for (const { idx, elements } of added) methods.insert(idx, elements, extra);
			},
			setScrollTop(v) {
				if (!$elm) return;
				$elm.scrollTop = v;
			},
			getParent() {
				if (!$elm) return null;
				return $elm.parentElement;
			},
			destroy() {
				$elm = null;
				$children.length = 0;
				child_nodes.length = 0;
				child_elements.length = 0;
				_events = null;
			},
			trackChild(dom, element, vnode, index) {
				$children.splice(index, 0, dom);
				child_elements.splice(index, 0, element);
				child_nodes.splice(index, 0, vnode);
			},
			untrackChild(index) {
				$children.splice(index, 1);
				child_elements.splice(index, 1);
				child_nodes.splice(index, 1);
			},
			replaceTrackedChild(index, dom, element, vnode) {
				if (index < $children.length) {
					$children[index] = dom;
					child_elements[index] = element;
					child_nodes[index] = vnode;
				} else {
					$children.splice(index, 0, dom);
					child_elements.splice(index, 0, element);
					child_nodes.splice(index, 0, vnode);
				}
			}
		};
		return { methods };
	}
	/**
	* Count how many real DOM nodes an element produces when rendered.
	* Transparent components (show, fragment, for, match) don't produce wrapper nodes;
	* their children render directly into the parent.
	* Portal renders to document.body, not inline.
	* All other types produce exactly 1 DOM node.
	*/
	function countRenderedNodes(elm) {
		if (!elm) return 0;
		const t = elm.t;
		if (t === "fragment" || t === "portal" || t === "show" || t === "for" || t === "match") {
			if (!elm.children) return 0;
			let count = 0;
			for (const child of elm.children) count += countRenderedNodes(child);
			return count;
		}
		return 1;
	}
	function isFragment(elm) {
		return [
			"fragment",
			"show",
			"for",
			"match"
		].includes(elm.t);
	}
	function insertedAnchor(elm) {
		return [
			"show",
			"for",
			"match"
		].includes(elm.t);
	}
	function isEmptyNode(elm) {
		return ["portal"].includes(elm.t);
	}
	var BP_MIN$2 = {
		sm: "576px",
		md: "768px",
		lg: "1024px",
		xl: "1280px"
	};
	var _gridSeq = 0;
	function injectCSS$2(id, css) {
		let el = document.getElementById(id);
		if (!el) {
			el = document.createElement("style");
			el.id = id;
			document.head.appendChild(el);
		}
		el.textContent = css;
	}
	function normalizeContent(v) {
		return {
			between: "space-between",
			around: "space-around",
			evenly: "space-evenly",
			start: "start",
			end: "end"
		}[v] ?? v;
	}
	function normalizeFlow(v) {
		return {
			col: "column",
			"row-dense": "row dense",
			"col-dense": "column dense"
		}[v] ?? v;
	}
	function colsToTemplate(n) {
		return `repeat(${n},minmax(0,1fr))`;
	}
	function normalizeRows(v) {
		if (typeof v === "number") return `repeat(${v},minmax(0,1fr))`;
		if (/^\d+$/.test(String(v))) return `repeat(${v},minmax(0,1fr))`;
		return String(v);
	}
	function buildGridBpCSS(cls, colsBp) {
		const xs = colsBp.xs;
		let css = `.${cls}{display:grid;`;
		if (xs !== void 0) css += `grid-template-columns:${colsToTemplate(xs)};`;
		css += "}";
		for (const bp of [
			"sm",
			"md",
			"lg",
			"xl"
		]) {
			const n = colsBp[bp];
			if (n === void 0) continue;
			css += `@media(min-width:${BP_MIN$2[bp]}){.${cls}{grid-template-columns:${colsToTemplate(n)};}}`;
		}
		return css;
	}
	function DOMGrid(props) {
		const box$ = HostElement({
			$elm: null,
			t: "grid",
			build: props.build
		});
		return {
			...box$.methods,
			t: "grid",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const s = elm.state ?? {};
				const cols = s.cols;
				if (cols !== void 0 && typeof cols === "object") {
					const cls = `tl-grid-${++_gridSeq}`;
					injectCSS$2(`tl-grid-style-${cls}`, buildGridBpCSS(cls, cols));
					$elm.classList.add(cls);
				} else {
					$elm.style.display = "grid";
					if (cols !== void 0) $elm.style.gridTemplateColumns = colsToTemplate(cols);
				}
				if (s.rows !== void 0) $elm.style.gridTemplateRows = normalizeRows(s.rows);
				if (s.autoRows) $elm.style.gridAutoRows = s.autoRows;
				if (s.autoCols) $elm.style.gridAutoColumns = s.autoCols;
				if (s.flow) $elm.style.gridAutoFlow = normalizeFlow(s.flow);
				if (s.gapX !== void 0 || s.gapY !== void 0) {
					if (s.gapY !== void 0) $elm.style.rowGap = `${s.gapY}px`;
					if (s.gapX !== void 0) $elm.style.columnGap = `${s.gapX}px`;
				} else if (s.gap !== void 0) $elm.style.gap = `${s.gap}px`;
				if (s.placeItems) $elm.style.placeItems = s.placeItems;
				else {
					if (s.alignItems) $elm.style.alignItems = s.alignItems;
					if (s.justifyItems) $elm.style.justifyItems = s.justifyItems;
				}
				if (s.placeContent) $elm.style.placeContent = normalizeContent(s.placeContent);
				else {
					if (s.alignContent) $elm.style.alignContent = normalizeContent(s.alignContent);
					if (s.justifyContent) $elm.style.justifyContent = normalizeContent(s.justifyContent);
				}
				if (s.marginBottom !== void 0) $elm.style.marginBottom = `${s.marginBottom}px`;
				if (s.marginTop !== void 0) $elm.style.marginTop = `${s.marginTop}px`;
				if (s.marginLeft !== void 0) $elm.style.marginLeft = `${s.marginLeft}px`;
				if (s.marginRight !== void 0) $elm.style.marginRight = `${s.marginRight}px`;
				if (s.padding !== void 0) $elm.style.padding = `${s.padding}px`;
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $dom) {
				box$.methods.set$elm($dom);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMText(props) {
		const t = "text";
		let $text = null;
		let box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "text";
			},
			isDocumentFragment() {
				return false;
			},
			setText(v) {
				if (!$text) return;
				if (v !== void 0 && v !== null) $text.textContent = String(v);
				else $text.textContent = "";
			},
			removeChildren() {
				$text = null;
				box$.methods.destroy();
			},
			render(elm) {
				$text = document.createTextNode((() => {
					if (elm.state.value !== null && elm.state.value !== void 0) return String(elm.state.value);
					return "";
				})());
				box$.methods.set$elm($text);
				return $text;
			},
			hydrate(elm, $elm) {
				if ($elm instanceof Text) {
					$text = $elm;
					box$.methods.set$elm($text);
					box$.methods.setupEventListener(elm.events);
				}
			}
		};
	}
	var DEFAULT_STYLES = {
		info: {
			bg: "#5470c6",
			color: "white"
		},
		debug: {
			bg: "#dfa639",
			color: "white"
		},
		warn: {
			bg: "#e6a23c",
			color: "white"
		},
		error: {
			bg: "#f56c6c",
			color: "white"
		}
	};
	var SCOPE_STYLE = {
		bg: "#19be6b",
		color: "white"
	};
	function formatTime() {
		return (/* @__PURE__ */ new Date()).toLocaleTimeString("en-US", {
			hour12: false,
			hour: "2-digit",
			minute: "2-digit",
			second: "2-digit"
		});
	}
	function buildLogParts(props, level) {
		const { prefix = "CORE", scope = "", time = false, mode = "classic", prefixColor } = props;
		const levelStyle = DEFAULT_STYLES[level];
		const parts = [];
		const styles = [];
		const prefixBg = prefixColor || levelStyle.bg;
		if (mode === "verbose") {
			if (time) {
				parts.push(`%c[${formatTime()}]%c`);
				styles.push("color:#909399;", "font-weight:bold;");
			}
			parts.push(`%c${level.toUpperCase()}%c`);
			styles.push(`color:white;background:${levelStyle.bg};border-radius:2px;`, `color:${levelStyle.bg};`);
		} else if (mode === "classic") {
			parts.push(`%c ${prefix} %c ${scope || level.toUpperCase()} %c`);
			styles.push(`color:white;background:${prefixBg};border-top-left-radius:2px;border-bottom-left-radius:2px;`, `color:white;background:${SCOPE_STYLE.bg};border-top-right-radius:2px;border-bottom-right-radius:2px;`, `color:${SCOPE_STYLE.color};`);
		} else {
			parts.push(`%c${prefix}%c`);
			styles.push(`color:white;background:${prefixBg};border-radius:2px;`, `color:${levelStyle.color};`);
		}
		return [parts.join(" "), ...styles];
	}
	function Logger(props = {}) {
		const { scope = "" } = props;
		let currentProps = { ...props };
		const logMethods = {
			info(...args) {
				console.log(...buildLogParts(currentProps, "info"), ...args);
			},
			debug(...args) {
				console.log(...buildLogParts(currentProps, "debug"), ...args);
			},
			warn(...args) {
				console.warn(...buildLogParts(currentProps, "warn"), ...args);
			},
			error(...args) {
				console.error(...buildLogParts(currentProps, "error"), ...args);
			}
		};
		return {
			scope,
			...logMethods,
			log: logMethods.info,
			setPrefixColor(color) {
				currentProps = {
					...currentProps,
					prefixColor: color
				};
			}
		};
	}
	var logger$6 = Logger({
		prefix: "dom",
		scope: "show",
		prefixColor: "#ff6b6b"
	});
	function DOMShow(props) {
		const t = "show";
		const $anchor = document.createTextNode("");
		const box$ = HostElement({
			$elm: $anchor,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "reactive";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $fragment = box$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				return $fragment;
			},
			hydrate(elm, $elm, opt) {
				logger$6.log("hydrate", elm, opt.$parent, opt.offset, opt.idx);
				const $anchor = document.createTextNode("");
				box$.methods.set$elm($anchor);
				const $v = opt.$parent || $elm;
				const idx = opt.offset;
				const hydrated_child_nodes = [];
				const hydrated_child_elements = [];
				if ($v && $v instanceof HTMLElement && idx !== void 0) {
					if (elm.children) {
						const total_nodes = countRenderedNodes(elm);
						const $children = Array.from($v.childNodes);
						const $children_belong_me = $children.slice(idx, idx + total_nodes);
						box$.methods.set$childrne($children_belong_me);
						const $last = $children[idx + total_nodes];
						logger$6.log("find my $children", idx, $children_belong_me, $last);
						if ($last) $v.insertBefore($anchor, $last);
						else $v.appendChild($anchor);
						let offset = idx;
						let $child_offset = 0;
						for (let i = 0; i < elm.children.length; i += 1) {
							const child = elm.children[i];
							const $child = $children_belong_me[$child_offset];
							logger$6.log("each child", i, child, $child, offset);
							if (child) {
								hydrated_child_elements.push(child);
								const child$ = hydrate_node(child, $child, {
									$parent: $v,
									offset,
									idx: i
								});
								if (child$) {
									if (isEmptyNode(child)) {} else if (isFragment(child)) {
										const count_$children = child$.get$children().length;
										offset += count_$children;
										offset += insertedAnchor(child) ? 1 : 0;
										$child_offset += count_$children;
									} else {
										offset += 1;
										$child_offset += 1;
									}
									hydrated_child_nodes.push(child$);
								}
							}
						}
						box$.methods.setchildnode(hydrated_child_nodes);
						box$.methods.setchildrenelement(hydrated_child_elements);
					}
				}
			},
			getChildren: box$.methods.getChildren,
			buildChildren: box$.methods.buildChildren,
			insertChildren(children) {
				box$.methods.removeChildren();
				box$.methods.insertChildren(children);
			},
			removeChildren() {
				box$.methods.removeChildren();
			},
			getParent() {
				return $anchor.parentElement;
			}
		};
	}
	function DOMMatch(props) {
		const t = "match";
		const $anchor = document.createTextNode("");
		const common$ = HostElement({
			$elm: $anchor,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "reactive";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $fragment = common$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				return $fragment;
			},
			hydrate(elm, $dom) {}
		};
	}
	var logger$5 = Logger({
		prefix: "dom",
		scope: "for",
		prefixColor: "#ff6b6b"
	});
	function DOMFor(props) {
		const t = "for";
		const $anchor = document.createTextNode("");
		const box$ = HostElement({
			$elm: $anchor,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "reactive";
			},
			get$elm: box$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			insert(idx, children) {
				logger$5.log("insert", idx, children);
				return box$.methods.insert(idx, children);
			},
			remove(idx, count) {
				box$.methods.remove(idx, count);
			},
			refresh: box$.methods.refresh,
			move: box$.methods.move,
			swap: box$.methods.move,
			render(elm) {
				const $fragment = box$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				return $fragment;
			},
			hydrate(elm, $elm, opt) {
				logger$5.log("hydrate", elm, $elm, opt.$parent, opt.offset, opt.idx);
				const $anchor = document.createTextNode("");
				box$.methods.set$elm($anchor);
				const $v = opt.$parent || $elm;
				const idx = opt.offset;
				const hydrated_elements = [];
				const hydrated_child_nodes = [];
				if ($v && $v instanceof HTMLElement) {
					if (elm.children) {
						const total_nodes = countRenderedNodes(elm);
						const $children = Array.from($v.childNodes);
						const $children_belong_me = $children.slice(idx, idx + total_nodes);
						box$.methods.set$childrne($children_belong_me);
						const $last = $children[idx + total_nodes];
						logger$5.log("$children belong me", idx, $children_belong_me, $last);
						let offset = idx;
						let $child_offset = 0;
						for (let i = 0; i < elm.children.length; i += 1) {
							const child = elm.children[i];
							hydrated_elements.push(child);
							const prev_child = elm.children[i - 1];
							const $child = $children_belong_me[$child_offset];
							logger$5.log("each child", i, child, $child, offset, prev_child);
							if (child) {
								const child$ = hydrate_node(child, $child, {
									$parent: $v,
									offset,
									idx: i
								});
								if (child$) {
									if (isEmptyNode(child)) {} else if (isFragment(child)) {
										const count_$children = child$.get$children().length;
										offset += count_$children;
										offset += insertedAnchor(child) ? 1 : 0;
										$child_offset += count_$children;
									} else {
										offset += 1;
										$child_offset += 1;
									}
									hydrated_child_nodes.push(child$);
								}
							}
						}
						box$.methods.setchildnode(hydrated_child_nodes);
						box$.methods.setchildrenelement(hydrated_elements);
						if ($last) $v.insertBefore($anchor, $last);
						else $v.appendChild($anchor);
					}
				}
			}
		};
	}
	var logger$4 = Logger({
		prefix: "dom",
		scope: "fragment"
	});
	function DOMFragment(props) {
		const t = "fragment";
		let $anchor;
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: box$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				$anchor = document.createTextNode("");
				box$.methods.set$elm($anchor);
				const $fragment = box$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				return $fragment;
			},
			hydrate(elm, $elm, opt) {
				logger$4.log("[hydrate]0", elm, $elm, opt.$parent, opt.offset);
				const $anchor = document.createTextNode("");
				box$.methods.set$elm($anchor);
				const $v = opt.$parent || $elm;
				const idx = opt.offset;
				if ($v && $v instanceof HTMLElement && idx !== void 0) {
					const $children = Array.from($v.childNodes);
					const total_nodes = countRenderedNodes(elm);
					const $children_belong_me = $children.slice(idx, idx + total_nodes);
					logger$4.log("[hydrate]$children", $children_belong_me);
					box$.methods.set$childrne($children_belong_me);
					const child_nodes = [];
					const child_elements = [];
					if (elm.children) {
						let offset = idx;
						let $child_offset = 0;
						for (let i = 0; i < elm.children.length; i += 1) {
							const child = elm.children[i];
							const $child = $children_belong_me[$child_offset];
							logger$4.log("[hydrate]each child", i, child, $child, offset, $child_offset);
							child_elements[i] = child;
							if (child) {
								const child$ = hydrate_node(child, $child, {
									$parent: $v,
									offset,
									idx: i
								});
								if (child$) if (isEmptyNode(child)) {} else if (isFragment(child)) {
									const count_$children = child$.get$children().length;
									offset += count_$children;
									offset += insertedAnchor(child) ? 1 : 0;
									$child_offset += count_$children;
								} else {
									offset += 1;
									$child_offset += 1;
								}
								child_nodes[i] = child$;
							}
						}
					}
					box$.methods.setchildnode(child_nodes);
					box$.methods.setchildrenelement(child_elements);
				}
			}
		};
	}
	function isDOMFragment(value) {
		return value.t === "fragment";
	}
	function DOMLazyView(props) {
		const t = "lazy-view";
		const $anchor = document.createTextNode("");
		const common$ = HostElement({
			$elm: $anchor,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return true;
			},
			setupEventListener() {},
			teardownEventListener() {},
			render(elm) {
				common$.methods.set$elm($anchor);
				const $fragment = common$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				return $fragment;
			},
			hydrate(elm, $dom) {},
			replaceChildren(children) {
				this.removeChildren();
				const $parent = $anchor.parentElement;
				if (!$parent) {
					console.warn("[]-view parent is null");
					return;
				}
				const r = common$.methods.buildChildren(children);
				common$.methods.setchildnode(r.child_nodes);
				common$.methods.set$childrne(r.child_host_nodes);
				common$.methods.setchildrenelement(r.child_elements);
				r.$fragment.appendChild($anchor);
				if ($parent) $parent.appendChild(r.$fragment);
				console.log("[timeless-dom]replaceChildren - before handleElementsMounted");
				setTimeout(() => {
					common$.methods.handleElementsMounted();
				}, 0);
			}
		};
	}
	function DOMImg(props) {
		const t = "img";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("img");
				if (elm.state.src) $elm.src = elm.state.src;
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state);
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			},
			setSrc(v) {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.src = v;
			}
		};
	}
	function DOMVideo(props) {
		const t = "video";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		const setupVideoEventListener = ($elm, events) => {
			if (!events) return;
			for (const [key, type] of [
				["onLoad", "load"],
				["onError", "error"],
				["onPlay", "play"],
				["onPause", "pause"],
				["onEnded", "ended"],
				["onTimeUpdate", "timeupdate"],
				["onLoadedMetadata", "loadedmetadata"]
			]) if (events[key]) $elm.addEventListener(type, events[key]);
		};
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("video");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				if (elm.state.src) $elm.src = elm.state.src;
				const $children = box$.methods.render(elm.children);
				if ($children) $elm.appendChild($children);
				box$.methods.setupEventListener(elm.events);
				setupVideoEventListener($elm, elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				setupVideoEventListener($elm, elm.events);
			},
			setSrc(v) {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.src = v;
			}
		};
	}
	function DOMAudio(props) {
		const t = "audio";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		const setupAudioEventListener = ($elm, events) => {
			if (!events) return;
			for (const [key, type] of [
				["onLoad", "load"],
				["onError", "error"],
				["onPlay", "play"],
				["onPause", "pause"],
				["onEnded", "ended"],
				["onTimeUpdate", "timeupdate"],
				["onLoadedMetadata", "loadedmetadata"]
			]) if (events[key]) $elm.addEventListener(type, events[key]);
		};
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("audio");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				if (elm.state.src) $elm.src = elm.state.src;
				const $children = box$.methods.render(elm.children);
				if ($children) $elm.appendChild($children);
				box$.methods.setupEventListener(elm.events);
				setupAudioEventListener($elm, elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				setupAudioEventListener($elm, elm.events);
			},
			setSrc(v) {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.src = v;
			}
		};
	}
	function DOMIcon(props) {
		const t = "icon";
		let $elm = null;
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const name = elm.state.name;
				if (!name) {
					console.warn(`Icon must have a name`);
					return null;
				}
				const pascal_name = name;
				const asn_node = (0, _timeless_timeless.getIconRegistry)()[pascal_name];
				if (!asn_node) {
					$elm = document.createElementNS("http://www.w3.org/2000/svg", "svg");
					$elm.setAttribute("width", String(elm.state.size || 24));
					$elm.setAttribute("height", String(elm.state.size || 24));
					$elm.setAttribute("viewBox", "0 0 24 24");
					$elm.style.backgroundColor = "rgba(255, 0, 0, 0.3)";
					$elm.style.display = "inline-block";
					$elm.style.borderRadius = "4px";
					$elm.setAttribute("data-name", pascal_name);
					common$.methods.set$elm($elm);
					return $elm;
				}
				$elm = render_asn_to_svg(asn_node, elm.state);
				common$.methods.set$elm($elm);
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
			}
		};
	}
	function render_asn_to_svg(asn, props) {
		const { tag, attrs = {}, children } = asn;
		const element = document.createElementNS("http://www.w3.org/2000/svg", tag);
		for (const [key, value] of Object.entries(attrs)) element.setAttribute(key, value);
		if (tag === "svg") {
			const size = props.size ? String(props.size) : "24";
			element.setAttribute("width", size);
			element.setAttribute("height", size);
			if (props.styleSet) element.classList.add(...props.styleSet);
		}
		if (children && children.length > 0) for (const child of children) {
			const childElement = render_asn_to_svg(child, props);
			element.appendChild(childElement);
		}
		return element;
	}
	function DOMInput(props) {
		const t = "input";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("input");
				$elm.type = "text";
				$elm.value = elm.state.value;
				if (elm.state.id) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				if (elm.state.placeholder) $elm.placeholder = elm.state.placeholder;
				if (elm.state.disabled) $elm.disabled = elm.state.disabled;
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				if (elm.state.maxLength !== void 0) $elm.setAttribute("max-length", elm.state.maxLength);
				if (elm.state.tabindex !== void 0) $elm.setAttribute("tabindex", elm.state.tabindex);
				if (elm.state.autoComplete !== void 0) $elm.setAttribute("autocomplete", elm.state.autoComplete);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("input", function(event) {
					event.preventDefault();
					if (events && events.onInput) events.onInput(event);
				});
				if (events) {
					if (events.onChange) $elm.addEventListener("change", events.onChange);
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
					if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				console.log("[dom]host/input hydrate", $elm, elm.state.value);
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("input", function(event) {
					console.log("handle input in hydrate");
					event.preventDefault();
					if (events && events.onInput) events.onInput(event);
				});
				if (events) {
					if (events.onChange) $elm.addEventListener("change", events.onChange);
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
					if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				}
			},
			setValue(value) {
				const $elm = common$.methods.get$elm();
				if (!$elm) {
					console.warn("DOMInput setValue: $elm is null");
					return;
				}
				$elm.value = value;
			}
		};
	}
	function DOMButton(props) {
		const t = "button";
		const $elm = document.createElement("button");
		const common$ = HostElement({
			$elm,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				common$.methods.applyState(elm.state, { initial: true });
				const $fragment = common$.methods.render(elm.children);
				common$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				if ($elm instanceof Text) return;
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
				if (elm.children) {
					const child_nodes = [];
					const $children = Array.from($elm.childNodes);
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						if (child) {
							const child$ = hydrate_node(child, $children[i], {
								$parent: $elm,
								offset: 0,
								idx: i
							});
							if (child$) child_nodes.push(child$);
						}
					}
					common$.methods.setchildnode(child_nodes);
				}
			}
		};
	}
	var logger$3 = Logger({
		prefix: "dom",
		scope: "portal"
	});
	function DOMPortal(props) {
		const t = "portal";
		const $anchor = document.createTextNode("");
		const box$ = HostElement({
			$elm: $anchor,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: box$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				const $fragment = box$.methods.render(elm.children);
				$fragment.appendChild($anchor);
				console.log("[DOMPortal.render] appending to body, children count=", $fragment.childNodes.length);
				document.body.appendChild($fragment);
				return $fragment;
			},
			hydrate(elm, $dom, opt) {
				const $v = $dom;
				const idx = 0;
				const child_nodes = [];
				const child_elements = [];
				if (elm.children) {
					const $children = [...Array.from(document.body.childNodes)].slice(1);
					const total_nodes = countRenderedNodes(elm);
					const $children_belong_me = $children.slice(idx, idx + total_nodes);
					logger$3.log("[]children belong me", $children, $children_belong_me, idx, total_nodes);
					box$.methods.set$childrne($children_belong_me);
					let offset = idx;
					let $child_offset = 0;
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						const $child = $children_belong_me[$child_offset];
						logger$3.log("each child", i, child, $child, offset);
						if (child) {
							child_elements.push(child);
							const child$ = hydrate_node(child, $child, {
								$parent: $v,
								offset,
								idx: i
							});
							if (child$) {
								if (isEmptyNode(child)) {} else if (isFragment(child)) {
									const count_$children = child$.get$children().length;
									offset += count_$children;
									offset += insertedAnchor(child) ? 1 : 0;
									$child_offset += count_$children;
								} else {
									offset += 1;
									$child_offset += 1;
								}
								child_nodes.push(child$);
							}
						}
					}
				}
				box$.methods.setchildnode(child_nodes);
				box$.methods.setchildrenelement(child_elements);
			},
			removeChildren() {
				const child_nodes = box$.methods.getChildren();
				for (const child of child_nodes) if (child) child.removeChildren();
				const $children = box$.methods.get$children();
				if ($children.length) {
					const $parent = $children[0].parentElement;
					logger$3.log("remove children", $children, $parent);
					if ($parent) for (let i = 0; i < $children.length; i += 1) {
						const $child = $children[i];
						if ($child && $child.parentElement === $parent) $parent.removeChild($child);
					}
				}
			}
		};
	}
	function DOMPopper(props) {
		const t = "popper";
		const $elm = document.createElement("div");
		const common$ = HostElement({
			$elm,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			get $elm() {
				return $elm;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				if (elm.state) common$.methods.setStyle({
					"z-index": elm.state.zIndex,
					position: "fixed",
					left: 0,
					top: 0,
					opacity: elm.state.placed ? 1 : 0,
					"pointer-events": elm.state.placed ? "initial" : "none",
					transform: elm.state.placed ? `translate3d(${Math.round(elm.state.x)}px, ${Math.round(elm.state.y)}px, 0)` : "translate3d(0, 0, 0)"
				});
				const $fragments = common$.methods.render(elm.children);
				$elm.appendChild($fragments);
				common$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $dom) {}
		};
	}
	function DOMCheckbox(props) {
		const t = "checkbox";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("input");
				console.log("[dom]create checkbox", elm.state);
				$elm.type = "checkbox";
				if (elm.state !== void 0) {
					$elm.checked = !!elm.state.checked;
					if (elm.state.checked) $elm.setAttribute("checked", "checked");
				}
				if (elm.state.id) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				if (elm.state.tabindex !== void 0) $elm.setAttribute("tabindex", elm.state.tabindex);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("click", function(event) {
					if (events && events.onChange) events.onChange(event);
				});
				if (events) {
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("click", function(event) {
					console.log("click", event);
					if (events && events.onChange) events.onChange(event);
				});
				if (events) {
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				}
			},
			setChecked(checked) {
				const $elm = common$.methods.get$elm();
				if (!$elm) {
					console.warn("DOMInput setValue: $elm is null");
					return;
				}
				if (!checked) $elm.removeAttribute("checked");
				else $elm.setAttribute("checked", "checked");
				$elm.checked = checked;
			}
		};
	}
	function DOMRadio(props) {
		const t = "radio";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			buildChildren: common$.methods.buildChildren,
			t,
			getType() {
				return "input";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("input");
				$elm.type = "radio";
				$elm.checked = !!elm.state.value;
				if (elm.state.id) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				if (elm.state.tabindex !== void 0) $elm.setAttribute("tabindex", elm.state.tabindex);
				delete elm.state.value;
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("click", function(event) {
					if (events && events.onChange) events.onChange(event);
				});
				if (events) {
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("click", function(event) {
					if (events && events.onChange) events.onChange(event);
				});
				if (events) {
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				}
			},
			setChecked(checked) {
				const $elm = common$.methods.get$elm();
				if (!$elm) {
					console.warn("DOMRadio setChecked: $elm is null");
					return;
				}
				if (!checked) $elm.removeAttribute("checked");
				else $elm.setAttribute("checked", "checked");
				$elm.checked = checked;
			}
		};
	}
	function DOMLabel(props) {
		const t = "label";
		const $elm = document.createElement("label");
		const common$ = HostElement({
			$elm,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				if (elm.state.for) common$.methods.setAttribute("for", elm.state.for);
				common$.methods.setupEventListener(elm.events);
				const $fragment = common$.methods.render(elm.children);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $dom) {
				common$.methods.set$elm($dom);
				common$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMTextarea(props) {
		const t = "textarea";
		const $elm = document.createElement("textarea");
		const common$ = HostElement({
			$elm,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			setValue(value) {
				$elm.value = value;
			},
			render(elm) {
				common$.methods.applyState(elm.state, { initial: true });
				$elm.value = elm.state.value;
				if (elm.state.id) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				if (elm.state.placeholder) $elm.placeholder = elm.state.placeholder;
				if (elm.state.disabled) $elm.disabled = elm.state.disabled;
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				if (events) {
					if (events.onInput) $elm.addEventListener("input", events.onInput);
					if (events.onChange) $elm.addEventListener("change", events.onChange);
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
					if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMFilePicker(props) {
		const t = "file-picker";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("input");
				$elm.type = "file";
				if (elm.state.id) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				if (elm.state.placeholder) $elm.placeholder = elm.state.placeholder;
				if (elm.state.disabled) $elm.disabled = elm.state.disabled;
				if (elm.state.accept) $elm.accept = elm.state.accept;
				if (elm.state.multiple) $elm.multiple = elm.state.multiple;
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				if (events) {
					if (events.onChange) $elm.addEventListener("change", events.onChange);
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
					if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
			},
			setValue(value) {},
			focus() {},
			blur() {}
		};
	}
	function DOMNumberInput(props) {
		const t = "number-input";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "input";
			},
			get$elm: box$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("input");
				$elm.type = "text";
				if (elm.state.id !== void 0) $elm.id = elm.state.id;
				if (elm.state.name) $elm.name = elm.state.name;
				if (elm.state.value !== void 0) $elm.value = elm.state.value;
				if (elm.state.placeholder !== void 0) $elm.placeholder = elm.state.placeholder;
				if (elm.state.disabled !== void 0) $elm.disabled = elm.state.disabled;
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				const events = elm.events;
				if (events) {
					if (events.onInput) $elm.addEventListener("input", events.onInput);
					if (events.onChange) $elm.addEventListener("change", events.onChange);
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
					if (events.onKeyDown) $elm.addEventListener("keydown", events.onKeyDown);
				}
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			},
			setValue(value) {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.value = value;
			},
			focus() {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.focus();
			},
			blur() {
				const $elm = box$.methods.get$elm();
				if ($elm) $elm.blur();
			}
		};
	}
	function DOMSelect(props) {
		const t = "select";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("select");
				common$.methods.applyState(elm.state, { initial: true });
				const r = common$.methods.buildChildren(elm.children);
				const $placeholder = document.createElement("option");
				$placeholder.value = "";
				$placeholder.innerText = elm.state.placeholder;
				r.$fragment.insertBefore($placeholder, r.$fragment.children[0]);
				$elm.appendChild(r.$fragment);
				common$.methods.setupEventListener(elm.events);
				$elm.addEventListener("change", function(event) {
					if (elm.events?.onChange) elm.events?.onChange(event);
				});
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMSelectOption(props) {
		let $elm = null;
		const t = "select-option";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			select() {
				if (!$elm) return;
				$elm.selected = true;
				$elm.setAttribute("selected", "");
			},
			unselect() {
				if (!$elm) return;
				$elm.selected = false;
				$elm.removeAttribute("selected");
			},
			render(elm) {
				$elm = document.createElement("option");
				if (elm.state) {
					if (elm.state.value) $elm.value = elm.state.value;
					if (elm.state.label) $elm.innerText = elm.state.label;
					if (elm.state.selected) {
						$elm.selected = true;
						$elm.setAttribute("selected", "");
					}
					if (elm.state.disabled) {
						$elm.disabled = true;
						$elm.setAttribute("disabled", "");
					}
				}
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMSelectOptionGroup(props) {
		const t = "select-option-group";
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...common$.methods,
			t,
			getType() {
				return "view";
			},
			get$elm: common$.methods.get$elm,
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("optgroup");
				if (elm.state) {
					if (elm.state.label) $elm.label = elm.state.label;
				}
				const r = common$.methods.buildChildren(elm.children);
				$elm.appendChild(r.$fragment);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
			}
		};
	}
	var BP_MIN$1 = {
		sm: "576px",
		md: "768px",
		lg: "1024px",
		xl: "1280px"
	};
	var _rowSeq = 0;
	function injectCSS$1(id, css) {
		let el = document.getElementById(id);
		if (!el) {
			el = document.createElement("style");
			el.id = id;
			document.head.appendChild(el);
		}
		el.textContent = css;
	}
	function normalizeJustify$1(v) {
		return {
			start: "flex-start",
			end: "flex-end",
			between: "space-between",
			around: "space-around",
			evenly: "space-evenly"
		}[v] ?? v;
	}
	function normalizeAlign$1(v) {
		return {
			start: "flex-start",
			end: "flex-end"
		}[v] ?? v;
	}
	function buildRowBpCSS(cls, state) {
		const bps = state.breakpoints ?? {};
		const xs = bps.xs ?? {};
		const direction = xs.direction ?? state.direction ?? "row";
		const gap = xs.gap ?? state.gap;
		const wrap = xs.wrap ?? state.wrap;
		const align = xs.align ?? state.align;
		const justify = xs.justify ?? state.justify;
		let css = `.${cls}{display:flex;flex-direction:${direction};`;
		if (gap !== void 0) css += `gap:${gap}px;`;
		if (wrap) css += `flex-wrap:wrap;`;
		if (align) css += `align-items:${normalizeAlign$1(align)};`;
		if (justify) css += `justify-content:${normalizeJustify$1(justify)};`;
		css += "}";
		for (const bp of [
			"sm",
			"md",
			"lg",
			"xl"
		]) {
			const cfg = bps[bp];
			if (!cfg) continue;
			let inner = "";
			if (cfg.direction) inner += `flex-direction:${cfg.direction};`;
			if (cfg.gap !== void 0) inner += `gap:${cfg.gap}px;`;
			if (cfg.wrap !== void 0) inner += `flex-wrap:${cfg.wrap ? "wrap" : "nowrap"};`;
			if (cfg.align) inner += `align-items:${normalizeAlign$1(cfg.align)};`;
			if (cfg.justify) inner += `justify-content:${normalizeJustify$1(cfg.justify)};`;
			if (inner) css += `@media(min-width:${BP_MIN$1[bp]}){.${cls}{${inner}}}`;
		}
		return css;
	}
	function DOMRow(props) {
		const box$ = HostElement({
			$elm: null,
			t: "row",
			build: props.build
		});
		return {
			...box$.methods,
			t: "row",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const s = elm.state ?? {};
				if (s.breakpoints && Object.keys(s.breakpoints).length > 0) {
					const cls = `tl-row-${++_rowSeq}`;
					injectCSS$1(`tl-row-style-${cls}`, buildRowBpCSS(cls, s));
					$elm.classList.add(cls);
				} else {
					$elm.style.display = "flex";
					$elm.style.flexDirection = s.direction ?? "row";
					if (s.gap !== void 0) $elm.style.gap = `${s.gap}px`;
					if (s.wrap) $elm.style.flexWrap = "wrap";
					if (s.align) $elm.style.alignItems = normalizeAlign$1(s.align);
					if (s.justify) $elm.style.justifyContent = normalizeJustify$1(s.justify);
				}
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	var BP_MIN = {
		sm: "576px",
		md: "768px",
		lg: "1024px",
		xl: "1280px"
	};
	var _colSeq = 0;
	function injectCSS(id, css) {
		let el = document.getElementById(id);
		if (!el) {
			el = document.createElement("style");
			el.id = id;
			document.head.appendChild(el);
		}
		el.textContent = css;
	}
	function normalizeJustify(v) {
		return {
			start: "flex-start",
			end: "flex-end",
			between: "space-between",
			around: "space-around",
			evenly: "space-evenly"
		}[v] ?? v;
	}
	function normalizeAlign(v) {
		return {
			start: "flex-start",
			end: "flex-end"
		}[v] ?? v;
	}
	function normalizePadding(v) {
		if (typeof v === "number") return `${v}px`;
		return v;
	}
	function normalizeWidth(v) {
		if (typeof v === "number") return `${v}px`;
		return v;
	}
	function DOMColumn(props) {
		const box$ = HostElement({
			$elm: null,
			t: "column",
			build: props.build
		});
		return {
			...box$.methods,
			t: "column",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const s = elm.state ?? {};
				$elm.style.display = "flex";
				$elm.style.flexDirection = "column";
				if (s.gap !== void 0) $elm.style.gap = `${s.gap}px`;
				if (s.align) $elm.style.alignItems = normalizeAlign(s.align);
				if (s.justify) $elm.style.justifyContent = normalizeJustify(s.justify);
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function buildColBpCSS(cls, flexBp) {
		const xs = flexBp.xs;
		let css = `.${cls}{`;
		if (xs !== void 0) css += typeof xs === "number" ? `flex:${xs};` : `flex:${xs};`;
		css += "}";
		for (const bp of [
			"sm",
			"md",
			"lg",
			"xl"
		]) {
			const v = flexBp[bp];
			if (v === void 0) continue;
			const flex = typeof v === "number" ? String(v) : v;
			css += `@media(min-width:${BP_MIN[bp]}){.${cls}{flex:${flex};}}`;
		}
		return css;
	}
	function DOMCol(props) {
		const box$ = HostElement({
			$elm: null,
			t: "col",
			build: props.build
		});
		return {
			...box$.methods,
			t: "col",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const s = elm.state ?? {};
				const flex = s.flex;
				if (flex !== void 0 && typeof flex === "object") {
					const cls = `tl-col-${++_colSeq}`;
					injectCSS(`tl-col-style-${cls}`, buildColBpCSS(cls, flex));
					$elm.classList.add(cls);
				} else if (flex !== void 0) $elm.style.flex = typeof flex === "number" ? String(flex) : flex;
				if (s.width !== void 0) $elm.style.width = normalizeWidth(s.width);
				if (s.padding !== void 0) $elm.style.padding = normalizePadding(s.padding);
				if (s.span !== void 0) $elm.style.gridColumn = `span ${s.span} / span ${s.span}`;
				if (s.start !== void 0) $elm.style.gridColumnStart = String(s.start);
				if (s.end !== void 0) $elm.style.gridColumnEnd = String(s.end);
				if (s.rowSpan !== void 0) $elm.style.gridRow = `span ${s.rowSpan} / span ${s.rowSpan}`;
				if (s.rowStart !== void 0) $elm.style.gridRowStart = String(s.rowStart);
				if (s.rowEnd !== void 0) $elm.style.gridRowEnd = String(s.rowEnd);
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMLink(props) {
		const t = "link";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElement("a");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				if (elm.state.href) $elm.href = elm.state.href;
				if (elm.state.target) $elm.target = elm.state.target;
				if (elm.state.rel) $elm.rel = elm.state.rel;
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm, opt) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				if (elm.children) {
					const child_nodes = [];
					const child_elements = [];
					const $children = Array.from($elm.childNodes);
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						child_elements.push(child);
						const $child = $children[i];
						if (child) {
							const child$ = hydrate_node(child, $child, {
								initial: true,
								$parent: $elm,
								offset: 0,
								idx: i
							});
							if (child$) child_nodes.push(child$);
						}
					}
					box$.methods.setchildrenelement(child_elements);
					box$.methods.setchildnode(child_nodes);
				}
			}
		};
	}
	function DOMWebview(props) {
		const t = "webview";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("iframe");
				if (elm.state.href) $elm.src = elm.state.href;
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				console.log("[dom]host/view - hydrate", $elm, elm.state);
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function parseGridTemplate(template) {
		const columns = [];
		let current = "";
		let depth = 0;
		for (const char of template) {
			if (char === "(") depth++;
			else if (char === ")") depth--;
			else if (char === " " && depth === 0) {
				if (current) {
					columns.push(current);
					current = "";
				}
				continue;
			}
			current += char;
		}
		if (current) columns.push(current);
		return columns;
	}
	function setupDragResize($handler, $splitView, handlerElm, event) {
		if (handlerElm.events?.onPointerDown) handlerElm.events.onPointerDown(event);
		const isVertical = $splitView.getAttribute("data-split-direction") === "vertical";
		const templateProp = isVertical ? "grid-template-rows" : "grid-template-columns";
		const cursorStyle = isVertical ? "row-resize" : "col-resize";
		event.preventDefault();
		$handler.setPointerCapture(event.pointerId);
		let startPos = isVertical ? event.clientY : event.clientX;
		let startTemplate = $splitView.style[templateProp];
		document.body.style.cursor = cursorStyle;
		document.body.style.userSelect = "none";
		$handler.style.touchAction = "none";
		const onMove = (moveEvent) => {
			const currentPos = isVertical ? moveEvent.clientY : moveEvent.clientX;
			const delta = currentPos - startPos;
			const columns = parseGridTemplate(startTemplate);
			let handlerIndex = 0;
			let sibling = $handler.parentElement?.previousElementSibling;
			while (sibling) {
				if (sibling.hasAttribute("data-split-view-handler")) handlerIndex++;
				sibling = sibling.previousElementSibling;
			}
			const leftColIdx = handlerIndex * 2;
			const rightColIdx = handlerIndex * 2 + 2;
			if (leftColIdx >= columns.length || rightColIdx >= columns.length) return;
			const leftCol = columns[leftColIdx];
			const rightCol = columns[rightColIdx];
			const isLeftAuto = leftCol === "auto" || leftCol === "1fr";
			const isRightAuto = rightCol === "auto" || rightCol === "1fr";
			const rect = (idx) => $splitView.children[idx].getBoundingClientRect();
			const leftPx = isLeftAuto ? isVertical ? rect(leftColIdx).height : rect(leftColIdx).width : parseFloat(leftCol);
			const rightPx = isRightAuto ? isVertical ? rect(rightColIdx).height : rect(rightColIdx).width : parseFloat(rightCol);
			let newLeft = leftPx + delta;
			let newRight = rightPx - delta;
			const leftPanel = $splitView.children[leftColIdx];
			const rightPanel = $splitView.children[rightColIdx];
			const leftMinWidth = leftPanel ? parseInt(leftPanel.getAttribute("data-min-size") || "20") : 20;
			const rightMinWidth = rightPanel ? parseInt(rightPanel.getAttribute("data-min-size") || "20") : 20;
			if (newLeft < leftMinWidth) {
				newRight -= leftMinWidth - newLeft;
				newLeft = leftMinWidth;
			}
			if (newRight < rightMinWidth) {
				newLeft -= rightMinWidth - newRight;
				newRight = rightMinWidth;
			}
			columns[leftColIdx] = `${Math.round(newLeft)}px`;
			columns[rightColIdx] = `${Math.round(newRight)}px`;
			startTemplate = columns.join(" ");
			$splitView.style[templateProp] = startTemplate;
			startPos = currentPos;
		};
		const onUp = () => {
			document.body.style.cursor = "";
			document.body.style.userSelect = "";
			$handler.style.touchAction = "";
			$handler.removeEventListener("pointermove", onMove);
			$handler.removeEventListener("pointerup", onUp);
		};
		$handler.addEventListener("pointermove", onMove);
		$handler.addEventListener("pointerup", onUp);
	}
	function DOMSplitView(props) {
		const box$ = HostElement({
			$elm: null,
			t: "split-view",
			build: props.build
		});
		return {
			...box$.methods,
			t: "split-view",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				const isVertical = elm.state.direction === "vertical";
				$elm.setAttribute("data-split-view", "");
				$elm.setAttribute("data-split-direction", isVertical ? "vertical" : "horizontal");
				$elm.style.display = "grid";
				$elm.style.height = "100%";
				const columns = elm.state.panels.map((panel) => {
					if (panel.t === "split-handler") return "1px";
					if (typeof panel.state.size === "number") return `${panel.state.size}px`;
					if (panel.state.size === "auto") return "1fr";
					return String(panel.state.size);
				}).join(" ");
				const templateProp = isVertical ? "grid-template-rows" : "grid-template-columns";
				$elm.style[templateProp] = columns;
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMSplitPane(props) {
		const box$ = HostElement({
			$elm: null,
			t: "split-pane",
			build: props.build
		});
		return {
			...box$.methods,
			t: "split-pane",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				$elm.setAttribute("data-split-view-panel", "");
				if (elm.state.minSize) $elm.setAttribute("data-min-size", String(elm.state.minSize));
				Object.assign(elm.state.style, {
					overflow: "clip",
					width: "100%",
					height: "100%",
					"min-height": "0"
				});
				if (elm.state.minSize) {
					const isVertical = elm.state.direction === "vertical";
					elm.state.style[isVertical ? "min-height" : "min-width"] = `${elm.state.minSize}px`;
				}
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				$elm.style.position = "relative";
				$elm.style.zIndex = "0";
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				$elm.style.position = "relative";
				$elm.style.zIndex = "0";
			}
		};
	}
	function DOMSplitHandler(props) {
		const box$ = HostElement({
			$elm: null,
			t: "split-pane",
			build: props.build
		});
		return {
			...box$.methods,
			t: "split-handler",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				const $line = document.createElement("div");
				const $hover_zone = document.createElement("div");
				let hover_timer = null;
				let is_hover_highlighted = false;
				$elm.setAttribute("data-split-view-handler", "");
				const applyDirection = () => {
					if (elm.state.direction === "vertical") {
						Object.assign(elm.state.style, {
							display: "flex",
							width: "100%",
							height: "1px",
							"z-index": 2,
							position: "relative",
							overflow: "visible"
						});
						Object.assign($line.style, {
							width: "100%",
							height: "1px",
							"pointer-events": "none",
							"background-color": "color-mix(in srgb, CanvasText 20%, black)"
						});
						Object.assign($hover_zone.style, {
							position: "absolute",
							left: "0",
							right: "0",
							top: "-2px",
							bottom: "-2px",
							cursor: "row-resize",
							transition: "background-color 0.2s ease"
						});
					} else {
						Object.assign(elm.state.style, {
							display: "flex",
							width: "1px",
							height: "100%",
							"z-index": 2,
							position: "relative",
							overflow: "visible"
						});
						Object.assign($line.style, {
							width: "1px",
							height: "100%",
							"pointer-events": "none",
							"background-color": "color-mix(in srgb, CanvasText 20%, black)"
						});
						Object.assign($hover_zone.style, {
							position: "absolute",
							top: "0",
							bottom: "0",
							left: "-2px",
							right: "-2px",
							cursor: "col-resize",
							transition: "background-color 0.2s ease"
						});
					}
				};
				box$.methods.set$elm($elm);
				applyDirection();
				box$.methods.applyState(elm.state, { initial: true });
				$elm.style.zIndex = "2";
				const setHoverHighlight = (highlighted) => {
					is_hover_highlighted = highlighted;
					$hover_zone.style.backgroundColor = highlighted ? "#3376cd" : "transparent";
				};
				$hover_zone.addEventListener("pointerenter", () => {
					if (hover_timer) clearTimeout(hover_timer);
					hover_timer = setTimeout(() => {
						setHoverHighlight(true);
						hover_timer = null;
					}, 600);
				});
				$hover_zone.addEventListener("pointerleave", () => {
					if (hover_timer) {
						clearTimeout(hover_timer);
						hover_timer = null;
					}
					if (is_hover_highlighted) setHoverHighlight(false);
				});
				$hover_zone.addEventListener("pointerdown", function(event) {
					const $splitView = $hover_zone.closest("[data-split-view]");
					if ($splitView) setupDragResize($hover_zone, $splitView, elm, event);
				});
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($line);
				$elm.appendChild($hover_zone);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				$elm.style.zIndex = "2";
			}
		};
	}
	function DOMScrollView(props) {
		const box$ = HostElement({
			$elm: null,
			t: "scroll-view",
			build: props.build
		});
		return {
			...box$.methods,
			t: "scroll-view",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				$elm.setAttribute("data-scroll-view", "");
				Object.assign(elm.state.style, {
					"overflow-y": "auto",
					"max-height": "100%"
				});
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMTabView(props) {
		let $contents = null;
		let $track = null;
		let _animating = false;
		const box$ = HostElement({
			$elm: null,
			t: "tab-view",
			build: props.build
		});
		return {
			...box$.methods,
			t: "tab-view",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				$elm.setAttribute("data-tab-view", "");
				$elm.style.height = "100%";
				$elm.style.display = "flex";
				$elm.style.flexDirection = "column";
				const $tabs = document.createElement("div");
				$tabs.setAttribute("data-tab-view-tabs", "");
				$tabs.style.cssText = "display: flex;";
				if (elm.state.tabs) {
					const $tab_fragment = document.createDocumentFragment();
					for (let i = 0; i < elm.state.tabs.length; i += 1) {
						const t = elm.state.tabs[i];
						const $tab = document.createElement("div");
						$tab.setAttribute("data-tab-view-tab", t.tab);
						$tab.addEventListener("click", function(event) {
							elm.handleClickTab(t);
						});
						$tab.innerText = t.label;
						$tab_fragment.appendChild($tab);
					}
					$tabs.appendChild($tab_fragment);
				}
				$contents = document.createElement("div");
				$contents.setAttribute("data-tab-view-panels", "");
				$contents.style.cssText = "width: 100%; flex: 1; min-height: 0;";
				$track = document.createElement("div");
				$track.setAttribute("data-tab-view-track", "");
				$track.style.cssText = "display: flex; width: 100%; height: 100%; transition: transform 0.3s ease;";
				const $content = document.createElement("div");
				$content.setAttribute("data-tab-view-panel", "");
				$content.style.cssText = "flex-shrink: 0; width: 100%; height: 100%;";
				const $fragment = box$.methods.render(elm.children);
				$content.appendChild($fragment);
				$track.appendChild($content);
				$contents.appendChild($track);
				$elm.appendChild($tabs);
				$elm.appendChild($contents);
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			},
			switchPanel(children, options) {
				if (_animating) return;
				const forward = options.to > options.from;
				const $content = document.createElement("div");
				$content.setAttribute("data-tab-view-panel", "");
				$content.style.cssText = "flex-shrink: 0; width: 100%; height: 100%;";
				const $fragment = box$.methods.render(children);
				$content.appendChild($fragment);
				_animating = true;
				const onTransitionEnd = () => {
					$track.removeEventListener("transitionend", onTransitionEnd);
					if (forward) $track.removeChild($track.firstChild);
					else $track.removeChild($track.lastChild);
					$track.style.transition = "none";
					$track.style.transform = "translateX(0%)";
					$track.offsetWidth;
					$track.style.transition = "transform 0.3s ease";
					_animating = false;
				};
				$track.addEventListener("transitionend", onTransitionEnd);
				if (forward) {
					$track.appendChild($content);
					requestAnimationFrame(() => {
						$track.style.transform = "translateX(-100%)";
					});
				} else {
					$track.insertBefore($content, $track.firstChild);
					$track.style.transition = "none";
					$track.style.transform = "translateX(-100%)";
					$track.offsetWidth;
					$track.style.transition = "transform 0.3s ease";
					requestAnimationFrame(() => {
						$track.style.transform = "translateX(0%)";
					});
				}
			}
		};
	}
	function DOMTabPane(props) {
		const box$ = HostElement({
			$elm: null,
			t: "tab-pane",
			build: props.build
		});
		return {
			...box$.methods,
			t: "tab-pane",
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	Logger({
		prefix: "dom",
		scope: "list-view",
		prefixColor: "#ff6b6b"
	});
	function DOMListView(props) {
		const t = "list-view";
		let $content = null;
		let $viewport = null;
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "reactive";
			},
			get$elm: box$.methods.get$elm,
			isDocumentFragment() {
				return true;
			},
			insert(idx, children) {
				return box$.methods.insert(idx, children, { $parent: $viewport });
			},
			remove(idx, count) {
				box$.methods.remove(idx, count, { $parent: $viewport });
			},
			refresh(payload) {
				box$.methods.refresh(payload, { $parent: $viewport });
			},
			move: box$.methods.move,
			swap: box$.methods.move,
			reorderSlots(elements) {
				if (!$viewport) return;
				for (let i = 0; i < elements.length; i++) {
					const el = elements[i];
					if (el && $viewport.children[i] !== el) $viewport.insertBefore(el, $viewport.children[i] || null);
				}
			},
			setStyleValue(k, value) {
				if (k === "viewportOffset") {
					if ($viewport) $viewport.style.transform = `translateY(${value}px)`;
					return;
				}
				if (k === "viewportOpacity") {
					if ($viewport) $viewport.style.opacity = String(value);
					return;
				}
				if ($content) {
					$content.style[k] = `${value}px`;
					if (k === "height") {
						const newHeight = Number(value);
						const maxScroll = Math.max(0, newHeight - $content.clientHeight);
						if ($content.scrollTop > maxScroll) $content.scrollTop = maxScroll;
					}
				}
			},
			render(elm) {
				const $elm = document.createElement("div");
				$content = document.createElement("div");
				$viewport = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state);
				box$.methods.setupEventListener(elm.events);
				$elm.style.position = "relative";
				$elm.style.overflowY = "auto";
				if (elm.state.height) $content.style.height = `${elm.state.height}px`;
				$content.style.position = "relative";
				$viewport.style.position = "absolute";
				$viewport.style.top = "0";
				$viewport.style.left = "0";
				$viewport.style.right = "0";
				$elm.setAttribute("data-list-view-root", "");
				$content.setAttribute("data-list-view-content", "");
				$viewport.setAttribute("data-list-view-viewport", "");
				$elm.addEventListener("scroll", function(event) {
					const top = event.target.scrollTop;
					elm.events?.onScroll?.({ scrollTop: top });
				});
				const $fragment = box$.methods.render(elm.children);
				$viewport.appendChild($fragment);
				$content.appendChild($viewport);
				$elm.appendChild($content);
				return $elm;
			},
			hydrate(elm, $elm, opt) {
				const $anchor = document.createTextNode("");
				box$.methods.set$elm($anchor);
				const $v = opt.$parent || $elm;
				const idx = opt.offset;
				const hydrated_elements = [];
				const hydrated_child_nodes = [];
				if ($v && $v instanceof HTMLElement) {
					if (elm.children) {
						const total_nodes = countRenderedNodes(elm);
						const $children = Array.from($v.childNodes);
						const $children_belong_me = $children.slice(idx, idx + total_nodes);
						box$.methods.set$childrne($children_belong_me);
						const $last = $children[idx + total_nodes];
						let offset = idx;
						let $child_offset = 0;
						for (let i = 0; i < elm.children.length; i += 1) {
							const child = elm.children[i];
							hydrated_elements.push(child);
							elm.children[i - 1];
							const $child = $children_belong_me[$child_offset];
							if (child) {
								const child$ = hydrate_node(child, $child, {
									$parent: $v,
									offset,
									idx: i
								});
								if (child$) {
									if (isEmptyNode(child)) {} else if (isFragment(child)) {
										const count_$children = child$.get$children().length;
										offset += count_$children;
										offset += insertedAnchor(child) ? 1 : 0;
										$child_offset += count_$children;
									} else {
										offset += 1;
										$child_offset += 1;
									}
									hydrated_child_nodes.push(child$);
								}
							}
						}
						box$.methods.setchildnode(hydrated_child_nodes);
						box$.methods.setchildrenelement(hydrated_elements);
						if ($last) $v.insertBefore($anchor, $last);
						else $v.appendChild($anchor);
					}
				}
			}
		};
	}
	var logger$2 = Logger({
		prefix: "dom",
		scope: "list-item-view"
	});
	function DOMListItemView(props) {
		const t = "list-item-view";
		let $elm = null;
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return false;
			},
			insertChildren(children) {
				box$.methods.insertChildren(children, { $parent: $elm });
			},
			removeChildren() {
				box$.methods.removeChildren({ $parent: $elm });
			},
			render(elm) {
				$elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				$elm.setAttribute("data-list-view-item", "");
				if (!elm.state.bound) $elm.style.display = "none";
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				const child_nodes = [];
				const child_elements = [];
				if ($elm && elm.children) {
					const $children = Array.from($elm.childNodes);
					let offset = 0;
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						const $child = $children[offset];
						child_elements.push(child);
						logger$2.log("each child", i, child, $child, offset);
						if (child) {
							const child$ = hydrate_node(child, $child, {
								$parent: $elm,
								offset,
								idx: i
							});
							if (child$) {
								if (isEmptyNode(child)) {} else if (isFragment(child)) {
									const count_$children = child$.get$children().length;
									offset += count_$children;
									offset += insertedAnchor(child) ? 1 : 0;
								} else offset += 1;
								child_nodes.push(child$);
							}
						}
					}
					box$.methods.setchildrenelement(child_elements);
					box$.methods.setchildnode(child_nodes);
				}
			}
		};
	}
	function DOMSwitch(props) {
		const t = "switch";
		let $root = null;
		let $thumb = null;
		let $loading = null;
		const common$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		const state = {};
		return {
			...common$.methods,
			t,
			getType() {
				return "input";
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				$root = document.createElement("button");
				$root.style.cssText = "position: relative; display: inline-flex; flex-shrink: 0; width: 32px; height: 18px; padding: 0; border-width: 1px; border-color: transparent; border-radius: 3.40282e38px; background-color: buttonface; transition-property: background-color; transition-timing-function: cubic-bezier(.4,0,.2,1); transition-duration: .15s;";
				$thumb = document.createElement("div");
				$thumb.style.cssText = "display: block; width: 16px; height: 16px; border-radius: 3.40282e38px; background-color: CanvasText; pointer-events: none; transform: translateX(0); transition-property: transform,translate,scale,rotate; transition-timing-function: cubic-bezier(.4,0,.2,1); transition-duration: .15s;";
				$loading = document.createElement("div");
				if (elm.state !== void 0) {
					state.checked = elm.state.checked;
					state.loading = elm.state.loading;
					if (elm.state.checked) $root.setAttribute("checked", "checked");
					if (elm.state.id) $root.id = elm.state.id;
				}
				$root.appendChild($thumb);
				common$.methods.set$elm($root);
				common$.methods.applyState(elm.state, { initial: true });
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$root.addEventListener("click", function(event) {
					if (events && events.onChange) {
						if (event.target) event.target.checked = !elm.state.checked;
						events.onChange(event);
					}
				});
				if (events) {
					if (events.onFocus) $root.addEventListener("focus", events.onFocus);
					if (events.onBlur) $root.addEventListener("blur", events.onBlur);
				}
				return $root;
			},
			hydrate(elm, $elm) {
				common$.methods.set$elm($elm);
				common$.methods.setupEventListener(elm.events);
				const events = elm.events;
				$elm.addEventListener("click", function(event) {
					console.log("click", event);
					if (events && events.onChange) events.onChange(event);
				});
				if (events) {
					if (events.onFocus) $elm.addEventListener("focus", events.onFocus);
					if (events.onBlur) $elm.addEventListener("blur", events.onBlur);
				}
			},
			setChecked(checked) {
				console.log("[dom]switch - setChecked", checked);
				state.checked = checked;
				if (!$root || !$thumb) {
					console.warn("DOMInput setValue: $elm is null");
					return;
				}
				if (checked) {
					$thumb.style.transform = `translateX(calc(100% - 2px))`;
					setTimeout(() => {
						$root.style.backgroundColor = "CanvasText";
						$thumb.style.backgroundColor = "buttonface";
					}, 120);
				} else {
					$thumb.style.transform = `translateX(0)`;
					setTimeout(() => {
						$root.style.backgroundColor = "buttonface";
						$thumb.style.backgroundColor = "CanvasText";
					}, 120);
				}
			},
			setLoading(loading) {
				console.log("[dom]switch - setLoading", loading);
				state.loading = loading;
				if (!$thumb || !$loading) {
					console.warn("DOMInput setValue: $elm is null");
					return;
				}
				$loading.style.color = state.checked ? "#fff" : "#000";
				if (loading) $thumb.appendChild($loading);
				else $thumb.removeChild($loading);
			}
		};
	}
	var logger$1 = Logger({
		prefix: "dom",
		scope: "view"
	});
	function DOMWindow(props) {
		const t = "window";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElement("div");
				$elm.style.cssText = "width: 100vw; height: 100vh;";
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				console.log("[dom]host/view - hydrate", $elm, elm.state);
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				const child_nodes = [];
				const child_elements = [];
				if ($elm && elm.children) {
					const $children = Array.from($elm.childNodes);
					let offset = 0;
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						const $child = $children[offset];
						child_elements.push(child);
						logger$1.log("each child", i, child, $child, offset);
						if (child) {
							const child$ = hydrate_node(child, $child, {
								$parent: $elm,
								offset,
								idx: i
							});
							if (child$) {
								if (isEmptyNode(child)) {} else if (isFragment(child)) {
									const count_$children = child$.get$children().length;
									offset += count_$children;
									offset += insertedAnchor(child) ? 1 : 0;
								} else offset += 1;
								child_nodes.push(child$);
							}
						}
					}
					box$.methods.setchildrenelement(child_elements);
					box$.methods.setchildnode(child_nodes);
				}
			}
		};
	}
	function DOMSVG(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "svg");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMG(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "g");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMCircle(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "circle");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMRect(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "rect");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMPath(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "path");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				if (elm.state.d) $elm.setAttribute("d", elm.state.d);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMLine(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "line");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMPolyline(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "polyline");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMPolygon(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "polygon");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMEllipse(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "ellipse");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMSVGText(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "text");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMDefs(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "defs");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMSymbol(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "symbol");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMUse(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "use");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMLinearGradient(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "linearGradient");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMRadialGradient(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "radialGradient");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMStop(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return false;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "stop");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				box$.methods.setupEventListener(elm.events);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMMask(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "mask");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function DOMClipPath(props) {
		const t = "svg";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return t;
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElementNS("http://www.w3.org/2000/svg", "clipPath");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
			}
		};
	}
	function buildAndRender(elm) {
		const vnode = build(elm);
		return {
			vnode,
			dom: vnode.render(elm)
		};
	}
	function build(elm) {
		if (elm.t === "view") {
			const view$ = DOMView({ build });
			elm.$elm = view$;
			return view$;
		}
		if (elm.t === "text") {
			const text$ = DOMText({ build });
			elm.$elm = text$;
			return text$;
		}
		if (elm.t === "label") {
			const label$ = DOMLabel({ build });
			elm.$elm = label$;
			return label$;
		}
		if (elm.t === "fragment") {
			const fragment$ = DOMFragment({ build });
			elm.$elm = fragment$;
			return fragment$;
		}
		if (elm.t === "lazy-view") {
			const lazyView$ = DOMLazyView({ build });
			elm.$elm = lazyView$;
			return lazyView$;
		}
		if (elm.t === "popper") {
			const popper$ = DOMPopper({ build });
			elm.$elm = popper$;
			return popper$;
		}
		if (elm.t === "style") {
			const style$ = DOMStyle({ build });
			elm.$elm = style$;
			return style$;
		}
		if (elm.t === "icon") {
			const icon$ = DOMIcon({ build });
			elm.$elm = icon$;
			return icon$;
		}
		if (elm.t === "img") {
			const img$ = DOMImg({ build });
			elm.$elm = img$;
			return img$;
		}
		if (elm.t === "video") {
			const video$ = DOMVideo({ build });
			elm.$elm = video$;
			return video$;
		}
		if (elm.t === "audio") {
			const audio$ = DOMAudio({ build });
			elm.$elm = audio$;
			return audio$;
		}
		if (elm.t === "list-view") {
			const listview$ = DOMListView({ build });
			elm.$elm = listview$;
			return listview$;
		}
		if (elm.t === "list-item-view") {
			const listitemview$ = DOMListItemView({ build });
			elm.$elm = listitemview$;
			return listitemview$;
		}
		if (elm.t === "webview") {
			const webview$ = DOMWebview({ build });
			elm.$elm = webview$;
			return webview$;
		}
		if (elm.t === "grid") {
			const grid$ = DOMGrid({ build });
			elm.$elm = grid$;
			return grid$;
		}
		if (elm.t === "row") {
			const row$ = DOMRow({ build });
			elm.$elm = row$;
			return row$;
		}
		if (elm.t === "window") {
			const window$ = DOMWindow({ build });
			elm.$elm = window$;
			return window$;
		}
		if (elm.t === "column") {
			const column$ = DOMColumn({ build });
			elm.$elm = column$;
			return column$;
		}
		if (elm.t === "col") {
			const col$ = DOMCol({ build });
			elm.$elm = col$;
			return col$;
		}
		if (elm.t === "input") {
			const input$ = DOMInput({ build });
			elm.$elm = input$;
			return input$;
		}
		if (elm.t === "switch") {
			const switch$ = DOMSwitch({ build });
			elm.$elm = switch$;
			return switch$;
		}
		if (elm.t === "file-picker") {
			const file_picker$ = DOMFilePicker({ build });
			elm.$elm = file_picker$;
			return file_picker$;
		}
		if (elm.t === "number-input") {
			const number_input$ = DOMNumberInput({ build });
			elm.$elm = number_input$;
			return number_input$;
		}
		if (elm.t === "textarea") {
			const textarea$ = DOMTextarea({ build });
			elm.$elm = textarea$;
			return textarea$;
		}
		if (elm.t === "checkbox") {
			const checkbox$ = DOMCheckbox({ build });
			elm.$elm = checkbox$;
			return checkbox$;
		}
		if (elm.t === "radio") {
			const radio$ = DOMRadio({ build });
			elm.$elm = radio$;
			return radio$;
		}
		if (elm.t === "select") {
			const select$ = DOMSelect({ build });
			elm.$elm = select$;
			return select$;
		}
		if (elm.t === "select-option") {
			const option$ = DOMSelectOption({ build });
			elm.$elm = option$;
			return option$;
		}
		if (elm.t === "select-option-group") {
			const group$ = DOMSelectOptionGroup({ build });
			elm.$elm = group$;
			return group$;
		}
		if (elm.t === "button") {
			const button$ = DOMButton({ build });
			elm.$elm = button$;
			return button$;
		}
		if (elm.t === "link") {
			const link$ = DOMLink({ build });
			elm.$elm = link$;
			return link$;
		}
		if (elm.t === "portal") {
			const portal$ = DOMPortal({ build });
			elm.$elm = portal$;
			return portal$;
		}
		if (elm.t === "show") {
			const show$ = DOMShow({ build });
			elm.$elm = show$;
			return show$;
		}
		if (elm.t === "match") {
			const match$ = DOMMatch({ build });
			elm.$elm = match$;
			return match$;
		}
		if (elm.t === "for") {
			const for$ = DOMFor({ build });
			elm.$elm = for$;
			return for$;
		}
		if (elm.t === "split-view") {
			const splitView$ = DOMSplitView({ build });
			elm.$elm = splitView$;
			return splitView$;
		}
		if (elm.t === "split-pane") {
			const pane$ = DOMSplitPane({ build });
			elm.$elm = pane$;
			return pane$;
		}
		if (elm.t === "split-handler") {
			const handler$ = DOMSplitHandler({ build });
			elm.$elm = handler$;
			return handler$;
		}
		if (elm.t === "scroll-view") {
			const scrollView$ = DOMScrollView({ build });
			elm.$elm = scrollView$;
			return scrollView$;
		}
		if (elm.t === "tab-view") {
			const tabView$ = DOMTabView({ build });
			elm.$elm = tabView$;
			return tabView$;
		}
		if (elm.t === "tab-pane") {
			const tabPane$ = DOMTabPane({ build });
			elm.$elm = tabPane$;
			return tabPane$;
		}
		if (elm.t === "svg-svg") {
			const svg$ = DOMSVG({ build });
			elm.$elm = svg$;
			return svg$;
		}
		if (elm.t === "svg-g") {
			const g$ = DOMG({ build });
			elm.$elm = g$;
			return g$;
		}
		if (elm.t === "svg-circle") {
			const circle$ = DOMCircle({ build });
			elm.$elm = circle$;
			return circle$;
		}
		if (elm.t === "svg-rect") {
			const rect$ = DOMRect({ build });
			elm.$elm = rect$;
			return rect$;
		}
		if (elm.t === "svg-path") {
			const path$ = DOMPath({ build });
			elm.$elm = path$;
			return path$;
		}
		if (elm.t === "svg-line") {
			const line$ = DOMLine({ build });
			elm.$elm = line$;
			return line$;
		}
		if (elm.t === "svg-polyline") {
			const polyline$ = DOMPolyline({ build });
			elm.$elm = polyline$;
			return polyline$;
		}
		if (elm.t === "svg-polygon") {
			const polygon$ = DOMPolygon({ build });
			elm.$elm = polygon$;
			return polygon$;
		}
		if (elm.t === "svg-ellipse") {
			const ellipse$ = DOMEllipse({ build });
			elm.$elm = ellipse$;
			return ellipse$;
		}
		if (elm.t === "svg-text") {
			const text$ = DOMSVGText({ build });
			elm.$elm = text$;
			return text$;
		}
		if (elm.t === "svg-defs") {
			const defs$ = DOMDefs({ build });
			elm.$elm = defs$;
			return defs$;
		}
		if (elm.t === "svg-symbol") {
			const symbol$ = DOMSymbol({ build });
			elm.$elm = symbol$;
			return symbol$;
		}
		if (elm.t === "svg-use") {
			const use$ = DOMUse({ build });
			elm.$elm = use$;
			return use$;
		}
		if (elm.t === "svg-linear-gradient") {
			const linearGradient$ = DOMLinearGradient({ build });
			elm.$elm = linearGradient$;
			return linearGradient$;
		}
		if (elm.t === "svg-radial-gradient") {
			const radialGradient$ = DOMRadialGradient({ build });
			elm.$elm = radialGradient$;
			return radialGradient$;
		}
		if (elm.t === "svg-stop") {
			const stop$ = DOMStop({ build });
			elm.$elm = stop$;
			return stop$;
		}
		if (elm.t === "svg-mask") {
			const mask$ = DOMMask({ build });
			elm.$elm = mask$;
			return mask$;
		}
		if (elm.t === "svg-clippath") {
			const clipPath$ = DOMClipPath({ build });
			elm.$elm = clipPath$;
			return clipPath$;
		}
		return DOMView({ build });
	}
	/**
	* Hydrate a virtual node tree onto existing server-rendered DOM.
	* This reuses the existing DOM nodes and attaches event listeners and reactive subscriptions.
	*
	* @param vnode - The virtual node tree to hydrate
	* @param container - The DOM container element with server-rendered content
	*/
	function hydrate(vnode, container) {
		if (!vnode) {
			console.error("[Hydrate] Invalid vnode");
			return;
		}
		if (!container) {
			console.error("[Hydrate] Container element not found");
			return;
		}
		if (!container.firstChild) {
			if (!document.querySelector(`[data-timeless-portal="0"]`)) {
				console.warn("[Hydrate] No SSR content found, falling back to render");
				render(vnode, container);
			}
			const view$ = hydrate_node(vnode, container, {
				initial: true,
				$parent: container,
				offset: 0,
				idx: 0
			});
			if (vnode.onMounted) {
				console.log("before onMounted", vnode.t, view$);
				vnode.onMounted({ target: view$ });
			}
			return;
		}
		const view$ = hydrate_node(vnode, container.firstChild, {
			initial: true,
			$parent: container,
			offset: 0,
			idx: 0
		});
		console.log("before onMounted", vnode.t, view$);
		if (vnode.onMounted) vnode.onMounted({ target: view$ });
	}
	/**
	* Recursively hydrate a virtual node onto an existing DOM node.
	*/
	function hydrate_node(vnode, $elm, opt) {
		console.log("hydrate node", vnode.t, $elm, opt.$parent, opt.offset);
		if (vnode.t === "view") {
			if ($elm instanceof Text) return null;
			const view$ = build(vnode);
			vnode.$elm = view$;
			view$.hydrate(vnode, $elm, opt);
			return view$;
		}
		if (vnode.t === "text") {
			if (!($elm instanceof Text)) return null;
			const text$ = build(vnode);
			vnode.$elm = text$;
			text$.hydrate(vnode, $elm, opt);
			return text$;
		}
		if (vnode.t === "row") {
			const row$ = build(vnode);
			vnode.$elm = row$;
			row$.hydrate(vnode, $elm, opt);
			return row$;
		}
		if (vnode.t === "column") {
			const column$ = build(vnode);
			vnode.$elm = column$;
			column$.hydrate(vnode, $elm, opt);
			return column$;
		}
		if (vnode.t === "col") {
			const col$ = build(vnode);
			vnode.$elm = col$;
			col$.hydrate(vnode, $elm, opt);
			return col$;
		}
		if (vnode.t === "grid") {
			const grid$ = build(vnode);
			vnode.$elm = grid$;
			grid$.hydrate(vnode, $elm, opt);
			return grid$;
		}
		if (vnode.t === "fragment") {
			const fragment$ = build(vnode);
			vnode.$elm = fragment$;
			fragment$.hydrate(vnode, $elm, opt);
			return fragment$;
		}
		if (vnode.t === "portal") {
			const portal$ = build(vnode);
			vnode.$elm = portal$;
			portal$.hydrate(vnode, $elm, opt);
			return portal$;
		}
		if (vnode.t === "for") {
			const for$ = build(vnode);
			vnode.$elm = for$;
			for$.hydrate(vnode, $elm, opt);
			return for$;
		}
		if (vnode.t === "show") {
			const show$ = build(vnode);
			vnode.$elm = show$;
			show$.hydrate(vnode, $elm, opt);
			return show$;
		}
		if (vnode.t === "match") {
			const match$ = build(vnode);
			vnode.$elm = match$;
			match$.hydrate(vnode, $elm, opt);
			return match$;
		}
		if (vnode.t === "button") {
			const button$ = build(vnode);
			vnode.$elm = button$;
			button$.hydrate(vnode, $elm, opt);
			return button$;
		}
		if (vnode.t === "icon") {
			const icon$ = build(vnode);
			vnode.$elm = icon$;
			icon$.hydrate(vnode, $elm, opt);
			return icon$;
		}
		if (vnode.t === "img") {
			const img$ = build(vnode);
			vnode.$elm = img$;
			img$.hydrate(vnode, $elm, opt);
			return img$;
		}
		if (vnode.t === "label") {
			const label$ = build(vnode);
			vnode.$elm = label$;
			label$.hydrate(vnode, $elm, opt);
			return label$;
		}
		if (vnode.t === "input") {
			const input$ = build(vnode);
			vnode.$elm = input$;
			input$.hydrate(vnode, $elm, opt);
			return input$;
		}
		if (vnode.t === "checkbox") {
			const checkbox$ = build(vnode);
			vnode.$elm = checkbox$;
			checkbox$.hydrate(vnode, $elm, opt);
			return checkbox$;
		}
		if (vnode.t === "file-picker") {
			const file_picker$ = build(vnode);
			vnode.$elm = file_picker$;
			file_picker$.hydrate(vnode, $elm, opt);
			return file_picker$;
		}
		if (vnode.t === "number-input") {
			const number_input$ = build(vnode);
			vnode.$elm = number_input$;
			number_input$.hydrate(vnode, $elm, opt);
			return number_input$;
		}
		if (vnode.t === "select") {
			const select$ = build(vnode);
			vnode.$elm = select$;
			select$.hydrate(vnode, $elm, opt);
			return select$;
		}
		return null;
	}
	var logger = Logger({
		prefix: "dom",
		scope: "view"
	});
	function DOMView(props) {
		const t = "view";
		const box$ = HostElement({
			$elm: null,
			t,
			build: props.build
		});
		return {
			...box$.methods,
			t,
			getType() {
				return "view";
			},
			isDocumentFragment() {
				return true;
			},
			render(elm) {
				const $elm = document.createElement("div");
				box$.methods.set$elm($elm);
				box$.methods.applyState(elm.state, { initial: true });
				const $fragment = box$.methods.render(elm.children);
				box$.methods.setupEventListener(elm.events);
				$elm.appendChild($fragment);
				return $elm;
			},
			hydrate(elm, $elm) {
				console.log("[dom]host/view - hydrate", $elm, elm.state);
				box$.methods.set$elm($elm);
				box$.methods.setupEventListener(elm.events);
				const child_nodes = [];
				const child_elements = [];
				if ($elm && elm.children) {
					const $children = Array.from($elm.childNodes);
					let offset = 0;
					for (let i = 0; i < elm.children.length; i += 1) {
						const child = elm.children[i];
						const $child = $children[offset];
						child_elements.push(child);
						logger.log("each child", i, child, $child, offset);
						if (child) {
							const child$ = hydrate_node(child, $child, {
								$parent: $elm,
								offset,
								idx: i
							});
							if (child$) {
								if (isEmptyNode(child)) {} else if (isFragment(child)) {
									const count_$children = child$.get$children().length;
									offset += count_$children;
									offset += insertedAnchor(child) ? 1 : 0;
								} else offset += 1;
								child_nodes.push(child$);
							}
						}
					}
					box$.methods.setchildrenelement(child_elements);
					box$.methods.setchildnode(child_nodes);
				}
			}
		};
	}
	function isDOMView(value) {
		return value.t === "view" || value.getType() === "view";
	}
	/**
	* Render a TimelessElement into a DOM container.
	*
	* On first call for a given $root, builds and appends the element tree.
	* On subsequent calls (e.g. HMR), diffs against the previous element tree
	* and applies minimal DOM patches — no full re-render needed.
	*/
	function render(elm, $root, extra = {}) {
		if (!$root) {
			console.error("[Render] Root element not found");
			return;
		}
		if (!elm) {
			console.error("[Render] Element is null");
			return;
		}
		if (!(0, _timeless_timeless.isElement)(elm)) {
			console.error("[Render] Root Element can't be lazy element");
			return;
		}
		const prev = rootElements.get($root);
		if (prev) {
			(0, _timeless_timeless.patch)(prev, elm, { buildAndRender });
			rootElements.set($root, elm);
			return;
		}
		const host$ = build(elm);
		if (!host$) {
			console.error("[Render] Element render return null");
			return;
		}
		if (!isDOMView(host$) && !isDOMFragment(host$)) {
			console.error("[Render] Element render return non DOMView or DOMFragment");
			return;
		}
		const $elm = host$.render(elm);
		if (!$elm) return;
		$root.appendChild($elm);
		rootElements.set($root, elm);
		setTimeout(() => {
			if (typeof elm.onMounted === "function") elm.onMounted({
				reason: "append to $root",
				target: $elm
			});
		}, 0);
	}
	/** Track the current root element per container for diff-based HMR updates. */
	var rootElements = /* @__PURE__ */ new WeakMap();
	console.log("timeless.dom.version 0.26.0");
	function isBrowser() {
		return typeof window !== "undefined" && typeof document !== "undefined";
	}
	var zeroRect = {
		x: 0,
		y: 0,
		top: 0,
		left: 0,
		right: 0,
		bottom: 0,
		width: 0,
		height: 0
	};
	function numberOr(value, fallback) {
		return typeof value === "number" && Number.isFinite(value) ? value : fallback;
	}
	function normalizeRect(rect) {
		if (!rect) return { ...zeroRect };
		const x = numberOr(rect.x, numberOr(rect.left, 0));
		const y = numberOr(rect.y, numberOr(rect.top, 0));
		const width = numberOr(rect.width, 0);
		const height = numberOr(rect.height, 0);
		const left = numberOr(rect.left, x);
		const top = numberOr(rect.top, y);
		return {
			x,
			y,
			top,
			left,
			width,
			height,
			right: numberOr(rect.right, left + width),
			bottom: numberOr(rect.bottom, top + height)
		};
	}
	function unwrapElement(value) {
		if (value && typeof value === "object") {
			const maybeWrapped = value;
			if (maybeWrapped.$el) return maybeWrapped.$el;
			if (maybeWrapped.contextElement) return maybeWrapped.contextElement;
		}
		return value;
	}
	function isElement(value) {
		if (!isBrowser()) return false;
		return unwrapElement(value) instanceof Element;
	}
	function isHTMLElement(value) {
		if (!isBrowser()) return false;
		return unwrapElement(value) instanceof HTMLElement;
	}
	function getBoundingClientRect(element) {
		if (!element) return { ...zeroRect };
		if (typeof element.getRect === "function") return normalizeRect(element.getRect());
		const unwrapped = unwrapElement(element);
		if (typeof unwrapped.getBoundingClientRect === "function") return normalizeRect(unwrapped.getBoundingClientRect());
		return { ...zeroRect };
	}
	function getDimensions(element) {
		if (!element) return {
			width: 0,
			height: 0
		};
		const unwrapped = unwrapElement(element);
		if (isHTMLElement(unwrapped)) {
			const el = unwrapped;
			return {
				width: el.offsetWidth,
				height: el.offsetHeight
			};
		}
		const rect = getBoundingClientRect(element);
		return {
			width: rect.width,
			height: rect.height
		};
	}
	function getElementRects(args) {
		const { reference, floating } = args;
		return {
			reference: getBoundingClientRect(reference),
			floating: getBoundingClientRect(floating)
		};
	}
	function getClippingRect(_args) {
		if (!isBrowser()) return normalizeRect({
			x: 0,
			y: 0,
			width: Infinity,
			height: Infinity
		});
		return normalizeRect({
			x: 0,
			y: 0,
			width: window.innerWidth,
			height: window.innerHeight
		});
	}
	function getOffsetParent(element) {
		const unwrapped = unwrapElement(element);
		if (!isHTMLElement(unwrapped)) return null;
		return unwrapped.offsetParent || document.body;
	}
	function isRTL(element) {
		const unwrapped = unwrapElement(element);
		if (!isHTMLElement(unwrapped)) return false;
		const el = unwrapped;
		return getComputedStyle(el).direction === "rtl";
	}
	function getScale(element) {
		const unwrapped = unwrapElement(element);
		if (!isHTMLElement(unwrapped)) return {
			x: 1,
			y: 1
		};
		const el = unwrapped;
		const rect = el.getBoundingClientRect();
		return {
			x: rect.width > 0 ? Math.round(rect.width) / el.offsetWidth || 1 : 1,
			y: rect.height > 0 ? Math.round(rect.height) / el.offsetHeight || 1 : 1
		};
	}
	function getDocumentElement(element) {
		if (!isBrowser()) return null;
		const unwrapped = unwrapElement(element);
		if (unwrapped && isElement(unwrapped)) return unwrapped.ownerDocument?.documentElement || document.documentElement;
		return document.documentElement;
	}
	const platform = (0, _timeless_timeless.setPlatform)({
		addEventListener(type, handler, options) {
			if (isBrowser()) {
				window.addEventListener(type, handler, options);
				return () => window.removeEventListener(type, handler, options);
			}
			return () => {};
		},
		patchBodyStyle(style) {
			if (isBrowser()) Object.assign(document.body.style, style);
		},
		getViewportSize() {
			if (isBrowser()) return {
				width: window.innerWidth,
				height: window.innerHeight
			};
			return {
				width: 0,
				height: 0
			};
		},
		isBrowser,
		isElement,
		isHTMLElement,
		getBoundingClientRect,
		getDimensions,
		getElementRects,
		getClippingRect,
		getOffsetParent,
		isRTL,
		getScale,
		getDocumentElement
	});
	exports.build = build;
	exports.buildAndRender = buildAndRender;
	exports.hydrate = hydrate;
	exports.platform = platform;
	exports.render = render;
});

//# sourceMappingURL=timeless.dom.umd.min.js.map