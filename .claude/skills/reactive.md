---
description: "Look up Timeless reactive system API: ref, refobj, refarr, computed, derive, classNames, styleNames. Trigger when user asks about reactivity primitives, reactive bindings, or @timeless/reactive."
---

# @timeless/reactive 响应式系统查阅

用户询问响应式 API 时，**读取对应子文件**后再回答。

## 子文件索引

| 用户提到 | 读取文件 |
|---------|---------|
| ref, ref().as, ref().value | `.claude/skills/reactive/ref.md` |
| refobj, refObject, 响应式对象, assign | `.claude/skills/reactive/ref-object.md` |
| refarr, refArray, 响应式数组, push/pop/insert | `.claude/skills/reactive/ref-array.md` |
| computed, derive, combine, 派生值 | `.claude/skills/reactive/computed.md` |
| classNames, cn, styleNames, sn, CSS class 合并 | `.claude/skills/reactive/class-style.md` |

## 查阅流程

1. 查上表 → 读取子文件
2. 如需完整源码 → 子文件标注了源文件路径
