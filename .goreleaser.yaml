# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com

# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    - go generate ./...

builds:
  - id: cross
    env:
      - CGO_ENABLED=0
    goos:
      - windows
      - linux
    main: .
    binary: wx_video_download
    ldflags:
      - -s -w
      - -X main.Mode=release
    ignore:
      - goos: darwin
        goarch: "386"
      - goos: windows
        goarch: "386"

  - id: macos
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    main: .
    binary: wx_video_download
    ldflags:
      - -s -w
      - -X main.Mode=release
    hooks:
      post:
        - rcodesign sign --p12-file {{ .Env.MAC_CERT_P12_FILE }} --p12-password {{ .Env.MAC_CERT_PASSWORD }} --code-signature-flags runtime "{{ .Path }}"

archives:
  - id: default
    format: tar.gz
    name_template: "wx_video_download_{{ .Tag }}_{{ .Os }}_{{ if eq .Arch \"amd64\" }}x86_64{{ else if eq .Arch \"386\" }}x86{{ else }}{{ .Arch }}{{ end }}"
    files:
      - src: config/config.template.yaml
        dst: config.yaml
      - src: LICENSE
      - src: '!README*.md'
    format_overrides:
      - goos: windows
        format: zip
      - goos: darwin
        format: zip

upx:
  - enabled: true
    ids:
      - cross
    compress: best
    lzma: true

checksum:
  name_template: "wx_video_download_{{ .Tag }}_checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  draft: true
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
